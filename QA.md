# ผลตรวจ ChampionWays Prototype

## แชตกลุ่มกับเมนเทอร์ — 13 กันยายน 2569

- `npm.cmd test`: 128 ผ่าน, 10 ข้ามตามเงื่อนไขเดิมของชุดทดสอบ ครอบคลุมหน้าเดิมและแชตที่ 1440, 768 และ 390px
- `npm.cmd run build`: ผ่าน TypeScript และ Vite
- `db:migrate:test` และ `db:migrate` สำหรับฐานข้อมูล dev: ผ่าน migration เพิ่มตารางแชต ไม่มีการ migrate production
- แชตตรวจหลายบัญชี: มีเพียงเจ้าของที่เชิญ/นำสมาชิกออกได้ เมนเทอร์ต้องรับคำขอ ผู้รับคำเชิญต้องมีอีเมลที่ยืนยันแล้ว คำเชิญหมดอายุ/ถูกยกเลิกใช้ไม่ได้ และสมาชิกที่ถูกนำออกเปิดข้อความหรือไฟล์ต่อไม่ได้
- ตรวจไฟล์ผิดประเภท/เกินขนาด ข้อความว่าง การส่งซ้ำ การแบ่งหน้าและโหลดต่อ นัดหมายผิดสิทธิ์/ลิงก์ผิดโดเมน รวมทั้ง refresh หน้าสนทนา
- ตรวจภาพจริง `artifacts/chat-desktop.png`, `chat-tablet.png`, `chat-mobile.png`: ไม่มีหน้าล้น ผ่าน axe และปรับเมนูมือถือไม่ให้ตัดคำกลางชื่อ
- ข้อจำกัด: polling 3 วินาที, คำเชิญในเว็บเท่านั้น, ไฟล์ส่วนตัวไม่เกิน 2 MB เก็บในฐานข้อมูล, ลิงก์ Meet/Zoom ใส่เองและเปิดแยก ยังไม่มีการชำระเงินหรือสร้างห้องคอลผ่าน API ดู [chat.md](chat.md)

ตรวจวันที่ 12 กันยายน 2569 บน Windows, Node.js 24.13.1 และ Microsoft Edge (Chromium)

## ผลล่าสุด หลังเปลี่ยนไปใช้ดีไซน์ใหม่

- `npm.cmd run build` ผ่าน: ตรวจ TypeScript และสร้าง production assets สำเร็จ
- `npm.cmd test` ผ่าน 39/39 กรณี รวมการเริ่มและปิด Vite อัตโนมัติ
- ทดสอบที่ 1440×1000, 768×1024 และ 390×844
- axe ตรวจ WCAG 2 A/AA และ 2.1 AA ของหน้าแรก หน้ารายละเอียด และหน้าเมนเทอร์ ไม่พบ violation ในการตรวจอัตโนมัติ
- ไม่พบ JavaScript page error หรือ horizontal overflow ในทั้งสามหน้า ทั้งสามขนาด
- ฟอนต์ Anuphan, IBM Plex Sans Thai และ IBM Plex Sans โหลดเป็นไฟล์ในโปรเจกต์ มีเทสต์ตรวจว่าไม่มี `@font-face` ที่ชี้ไป CDN

## สิ่งที่แก้ระหว่างตรวจรอบนี้

- โฟกัสอัตโนมัติที่ `<main>` ทำให้เกิดกรอบ focus ล้อมทั้งหน้า และกด Tab ครั้งแรกไม่ถึง skip link แก้เป็นย้ายโฟกัสเฉพาะตอนเปลี่ยนหน้า โดยเทียบ path แทนการนับรอบเรนเดอร์ เพราะ StrictMode รัน effect ซ้ำ
- สามสีไม่ผ่านคอนทราสต์ AA คือเมนูที่ยังไม่เปิดใช้ (2.56), ข้อความใต้การ์ดเมนเทอร์ (4.15) และเลขลำดับหัวข้อในหน้ารายละเอียด (1.92) ปรับให้ผ่านทั้งหมด

## พฤติกรรมที่ตรวจ

ค้นหาไทย/อังกฤษ ตัดช่องว่างและไม่สนตัวพิมพ์, กรองหมวด, กรองใกล้ปิดรับ, เรียงลำดับสี่แบบ, แบ่งหน้า 6 เวทีต่อหน้า, URL ที่มีค่าตัวกรองหรือเลขหน้าไม่ถูกต้อง, บันทึกเวทีและตัวเลขบนหัวเว็บ, มุมมองเฉพาะที่บันทึกและการคงอยู่หลังรีเฟรช, เปิดรายละเอียดครบ 14 เวทีจาก URL ตรง, เนื้อหาครบ 5 ส่วน, กลับพร้อมคำค้น/ตัวกรอง/ตำแหน่งเลื่อนเดิม, slug ที่ไม่มีอยู่, ลิงก์จากหน้ารายละเอียดไปหน้าเมนเทอร์, ขั้นตอนจับคู่เมนเทอร์ตั้งแต่เลือกเวทีจนถึงพรีวิวการจอง, การจัดชั้นเมนเทอร์และการแยกคนที่คิวไม่ทันเดดไลน์, การหารราคาตามจำนวนสมาชิก, การปฏิเสธวันส่งงานที่เป็นอดีต, keyboard focus

## ภาพหน้าจอ

| ขนาด | หน้าแรก | หน้ารายละเอียด | หน้าเมนเทอร์ |
| --- | --- | --- | --- |
| Desktop | [ภาพ](artifacts/home-desktop.png) | [ภาพ](artifacts/detail-desktop.png) | [ภาพ](artifacts/mentors-desktop.png) |
| Tablet | [ภาพ](artifacts/home-tablet.png) | [ภาพ](artifacts/detail-tablet.png) | [ภาพ](artifacts/mentors-tablet.png) |
| Mobile | [ภาพ](artifacts/home-mobile.png) | [ภาพ](artifacts/detail-mobile.png) | [ภาพ](artifacts/mentors-mobile.png) |

## ตรวจการ build สำหรับ GitHub Pages

ตรวจซ้ำวันที่ 12 กันยายน 2569 หลังเปลี่ยนดีไซน์

build ด้วย `BASE_PATH=/championway/` แล้วเสิร์ฟ `dist/` ด้วยเซิร์ฟเวอร์จำลองที่ทำตัวแบบ GitHub Pages คือคืน `404.html` พร้อม status 404 เมื่อไม่พบไฟล์ จากนั้นตรวจด้วย Microsoft Edge ผ่านทุกข้อ

- เปิด URL หน้ารายละเอียดตรง ๆ แล้วได้เวทีที่ถูกต้อง ไม่ใช่หน้าว่าง
- หน้าแรกแสดงการ์ดครบ 6 ใบของหน้าแรก
- เปิดหน้าเมนเทอร์จาก URL ตรงได้
- slug ที่ไม่มีอยู่แสดงหน้า 404 ของเว็บ
- กดลิงก์ในเว็บแล้ว URL ยังคง prefix `/championway` ไว้
- favicon และ asset ทุกไฟล์โหลดได้ ไม่มี JavaScript error และไม่มี request ที่ล้มเหลว

`npm.cmd test` ยังผ่าน 39/39 เพราะ dev server ไม่ได้ตั้ง `BASE_PATH` จึงอยู่ที่ `/` เหมือนเดิม

## ขอบเขตผลตรวจ

ข้อมูลทุกเวทีและเมนเทอร์เป็นสมมติ ไม่มี backend การจอง หรือการชำระเงินจริง การตรวจอัตโนมัติไม่ได้ทดแทนการทดสอบกับผู้ใช้และ screen reader จริง ยังไม่ได้ทดสอบ Safari/Firefox

การตรวจ subpath ข้างต้นใช้เซิร์ฟเวอร์จำลอง ไม่ใช่ GitHub Pages จริง และยังไม่ได้ deploy จึงยังไม่ได้ยืนยันพฤติกรรมบนโฮสต์จริง

อันดับ “เวทีที่คนสนใจมากที่สุด” และ “เมนเทอร์ประจำสัปดาห์” เป็นลำดับที่กำหนดด้วยมือเพื่อการออกแบบ ไม่ได้มาจากการวัดการใช้งาน เช่นเดียวกับการจัดชั้นเมนเทอร์
## ตรวจหน้าสมัครเมนเทอร์ — 12 กันยายน 2569

- `npm.cmd test`: ผ่าน 57/57 เคส รวมหน้าเดิมและหน้าสมัครใหม่ บน Edge ที่ 390, 768 และ 1440px
- ตรวจเส้นทางสมัคร เปิด URL ตรง รีเฟรช ย้อนขั้นตอนโดยข้อมูลและไฟล์ยังอยู่ ตรวจข้อมูลบังคับ/หลักฐาน/ความถนัด/คิว และทดลองส่งโดยไม่มีคำขอส่งข้อมูลไปเซิร์ฟเวอร์
- ตรวจทั้ง 4 ขั้นตอนด้วย axe (WCAG 2 A/AA และ 2.1 AA) และตรวจว่าไม่มี horizontal overflow
- ดูภาพ render จริงและแก้ layout แท็บเล็ตกับปุ่มมือถือ จากนั้นทดสอบหน้าสมัครบนมือถือซ้ำผ่าน 6/6 เคส
- `npm.cmd run build`: ผ่าน

ภาพหน้าสมัคร: [Desktop](artifacts/application-desktop-step-1.png), [Tablet ขั้นบริการ](artifacts/application-tablet-step-3.png), [Mobile ขั้นตรวจทาน](artifacts/application-mobile-step-4.png) มีภาพครบทั้ง 4 ขั้นตอนใน `artifacts/application-*-step-*.png`

ยังเป็น prototype ไม่มี backend การตรวจหลักฐานหรือการส่งใบสมัครจริง ผลตรวจนี้ใช้ Edge ไม่ครอบคลุม Safari/Firefox และการทดสอบกับผู้ใช้จริง

## ตรวจการปรับดีไซน์ขั้นตอน 2–4 — 12 กันยายน 2569

- `npm.cmd test`: ผ่าน 63/63 เคส บน Edge ที่ 390, 768 และ 1440px รวมเคสใหม่สองเคสของขั้นตอนสมัคร
- ขั้นตอน 2–4 แบ่งเป็นกลุ่มด้วยเส้นแบ่ง พร้อมหัวข้อและคำอธิบายประจำกลุ่ม
- ขั้นตอน 3 ตรวจตัวนับความถนัด ราคาหารต่อคนที่อยู่ติดช่องกรอก และคิวสองกล่องที่แยกจากกัน
- ขั้นตอน 4 ตรวจปุ่ม “แก้ไข” ของทุกกลุ่มว่ากลับไปขั้นที่ถูกต้องโดยข้อมูลและไฟล์ยังอยู่
- ตรวจว่าติ๊กยอมรับไม่ครบแล้วส่งไม่ได้ focus ย้ายไปช่องแรกที่ขาด และการแก้ข้อมูลล้างเฉพาะข้อยืนยันความถูกต้อง
- axe (WCAG 2 A/AA และ 2.1 AA) ผ่านทั้ง 4 ขั้นตอน หลังแก้คอนทราสต์ของคำอธิบายราคาซึ่งเดิมได้ 4.48:1
- `npm.cmd run build`: ผ่าน

ยังไม่มี backend การตรวจหลักฐานหรือการส่งใบสมัครจริง เงื่อนไขที่ติ๊กเป็นข้อความออกแบบ flow ไม่ใช่สัญญาที่ระบบบังคับใช้ได้

## ตรวจโมเดลข้อมูลใหม่และระบบตัวกรอง — 12 กันยายน 2569

- `npm.cmd test` ผ่าน 69/69 กรณี ที่ 1440×1000, 768×1024 และ 390×844
- `npm.cmd run build` ผ่าน ทั้ง TypeScript และ production assets
- axe ตรวจ WCAG 2 A/AA และ 2.1 AA เพิ่มกรณี **แผงตัวกรองขณะเปิด** ไม่พบ violation
- เทสใหม่ตรวจกฎการรวมเงื่อนไข คือภายในกลุ่มเป็น “หรือ” ระหว่างกลุ่มเป็น “และ” และตรวจว่าลิงก์ที่แชร์เปิดมาแล้วตัวกรองยังติดอยู่ครบ
- เทสใหม่ตรวจว่าปุ่ม “ล้างทั้งหมด” ล้างเฉพาะตัวกรองในแผง โดยคงหมวดที่เลือกและคำค้นไว้
- ลิงก์เก่ารูปแบบ `?category=design` ยังเปิดได้ มีเทสคุมไว้

บั๊กที่เจอและแก้ระหว่างตรวจ

- อ่าน `pmax` จาก URL ที่ไม่มีคีย์นี้ได้ค่า 0 เพราะ `Number(null)` คือ 0 ทำให้หน้าแรกเหลือเวทีเดียว แก้โดยเช็กว่าคีย์มีอยู่จริงก่อนแปลงเป็นตัวเลข
- ระยะห่างใต้หัวข้อกลุ่มในแผงกว้างเกินไป เพราะ `legend` ระดับ global มี `margin-bottom` ซ้อนกับระยะที่ Chrome เว้นให้อยู่แล้ว
- เวที Circular Design Lab ตั้งชื่อสถานที่ซ้ำกับชื่อผู้จัด ทำให้บรรทัดสถานที่อ่านเป็นชื่อเดียวกันสองครั้ง

ข้อจำกัดที่ยังอยู่

- ข้อมูลทั้ง 14 รายการยังเป็นข้อมูลสมมติ `sourceUrl` จึงว่าง และหน้ารายละเอียดแสดงว่าไม่มีประกาศต้นทางให้เปิด
- หมวด `performing` `film` และ `food` ยังไม่มีรายการ ชิปกดแล้วจะเห็นหน้าว่าง

## ตรวจหน้าจัดการ — 12 กันยายน 2569

- `npm.cmd test` ผ่าน 90/90 กรณี ที่ 1440×1000, 768×1024 และ 390×844
- `npm.cmd run build` ผ่าน
- axe ตรวจ WCAG 2 A/AA และ 2.1 AA ครบทั้งห้าหน้าจัดการ ไม่พบ violation และไม่มี horizontal overflow
- เทสตรวจว่าทุกหน้าจัดการมีแถบเตือนว่ายังไม่มีระบบยืนยันตัวตนและสิทธิ์
- เทสตรวจว่าคิวเรียงตามรอนานสุดก่อน และกรองตามสถานะได้
- เทสตรวจว่าปุ่มเผยแพร่กดไม่ได้จนกว่าจะติ๊กรายการตรวจครบ และกลับมากดไม่ได้อีกเมื่อเอาติ๊กออก
- เทสตรวจว่าปุ่มไม่ผ่านและขอข้อมูลเพิ่มบังคับกรอกเหตุผล และย้าย focus ไปช่องเหตุผล
- เทสตรวจว่าอีเมลและนามสกุลเต็มของผู้สมัครเมนเทอร์ไม่อยู่ในบล็อกข้อมูลสาธารณะ

ข้อจำกัดที่ยังอยู่

- ไม่มี backend ไม่มี auth การกดปุ่มตัดสินไม่ถูกบันทึกและไม่ส่งอีเมลจริง
- ใบในคิวทั้งหมดเป็นข้อมูลตัวอย่าง ไม่ใช่ข้อมูลของบุคคลหรือหน่วยงานจริง
- `/admin` อยู่ใน bundle เดียวกับหน้าบ้าน ถ้า deploy ขึ้นโฮสต์สาธารณะจะเปิดได้จากอินเทอร์เน็ต

## ตรวจหลังต่อ backend — 12 กันยายน 2569

- `npm.cmd test` ผ่าน 86/86 กรณี ข้าม 4 กรณีโดยตั้งใจ (เทสที่เปลี่ยนข้อมูลจริงรันเฉพาะ desktop)
- `npm.cmd run build` ผ่าน ขนาด JS ลดจาก 451 KB เหลือ 410 KB เพราะข้อมูลตัวอย่างไม่ถูกรวมเข้า bundle อีกต่อไป
- axe ตรวจ WCAG 2 A/AA และ 2.1 AA ครบทุกหน้ารวมหน้าจัดการ ไม่พบ violation
- ทดสอบ API แยกต่างหาก 27 กรณี ครอบคลุมตัวกรอง การค้นภาษาไทย สิทธิ์ และการตัดสิน

สิ่งที่เทสยืนยันเพิ่มในรอบนี้

- หน้าจัดการปฏิเสธทั้งคนที่ยังไม่ล็อกอินและสมาชิกทั่วไป และ **API ปฏิเสธเองด้วย** ไม่ใช่แค่ซ่อนปุ่ม
- เผยแพร่ไม่ได้จนกว่ารายการตรวจจะครบ และกลับมากดไม่ได้อีกเมื่อเอาติ๊กออก
- ไม่ผ่านและขอข้อมูลเพิ่มบังคับกรอกเหตุผล และเหตุผลนั้นกลับมาแสดงในคิว
- ใบที่ผู้จัดส่งเข้ามาแล้วผ่านการตรวจ ปรากฏบนหน้าเว็บสาธารณะจริง
- ใบสมัครเมนเทอร์ถูกส่งครั้งเดียว และไม่มีการอัปโหลดไฟล์ไปที่ใด

บั๊กที่เจอและแก้ระหว่างตรวจ

- `sql` แบบ template ผูกอาเรย์เป็นพารามิเตอร์ตัวเดียว ทำให้เงื่อนไข `in` ผิด ต้องใช้ `inArray()` และ `exists()`
- slug ภาษาไทยถูกตัดสระและวรรณยุกต์ทิ้ง เพราะ regex เก็บแค่ `\p{L}` ไม่ได้เก็บ `\p{M}`
- เรียกโหลดซ้ำหลังตัดสินทำให้หน้าถูกถอดทิ้ง ข้อความยืนยันผลจึงหายไปทันที
- ลิงก์กลางย่อหน้าในใบสมัครแยกจากข้อความด้วยสีอย่างเดียว axe จับได้ แก้ด้วยการขีดเส้นใต้
- กดปุ่มดูเมนเทอร์ก่อนข้อมูลเวทีโหลดเสร็จแล้วไม่เกิดอะไรขึ้น แก้ด้วยการปิดปุ่มระหว่างโหลด

ข้อจำกัดที่ยังอยู่

- ข้อมูลเวที เมนเทอร์ และใบในคิวยังเป็นข้อมูลตัวอย่างทั้งหมด
- อีเมลยังไม่ถูกส่งออกจริง บันทึกลงตาราง `email_log` แทน
- ไฟล์แนบยังไม่ถูกอัปโหลด บันทึกเป็นชื่อไฟล์ไว้ก่อน
- ยังไม่มีหน้าให้ผู้จัดกรอกใบลงงานแข่ง ต้องยิง API ตรง
- GitHub Pages ใช้ไม่ได้แล้วเพราะรัน API ไม่ได้ workflow ถูกเปลี่ยนเป็นสั่งรันมือ
## การแยกฐาน dev/test — 13 กันยายน 2569

`npm.cmd run test:safety` ผ่าน 5/5 กรณี: ยอมรับฐานแยก, ปฏิเสธค่าที่ขาด, ปฏิเสธ production/Vercel, เทียบ Neon pooled/direct endpoint และไม่แสดง credential ในข้อความ URL ผิดรูปแบบ `npm.cmd run build` ผ่าน

ตรวจ `npm.cmd test` ด้วย environment ปัจจุบันแล้วหยุดก่อนเชื่อมต่อฐานเพราะไม่มี TEST_DATABASE_URL ตามที่ออกแบบ จึงยังไม่ได้รัน browser suite กับฐาน test จริง ไม่ได้ migrate/seed ฐานที่มีอยู่ และยังไม่ได้ยืนยัน Preview deployment ดูขั้นตอนใน [environments.md](environments.md)
## ยืนยันฐาน test แยก — 13 กันยายน 2569

หลังบันทึกค่า environment ตรวจ guard ผ่านและเชื่อมต่อฐาน test ได้ พบ schema-only clone มี 16 ตารางว่างและไม่มี migration records จึงตรวจชื่อตาราง/enum ตรงกับ migration แรกและจำนวนแถวทุกตารางเป็นศูนย์ ก่อนสร้างโครงสร้าง test ใหม่ด้วย migration มาตรฐานสำเร็จ ไม่ได้แก้ฐาน dev/production

พอร์ต 5174 มีโปรแกรมอื่นใช้ จึงเพิ่ม TEST_WEB_PORT / TEST_API_PORT และตั้งในเครื่องเป็น 15174 / 18788 โดยไม่หยุดโปรแกรมเดิม ชุดทดสอบเต็มได้ 85 ผ่าน 4 ข้าม และ 1 timeout ระหว่าง Vite reload จากการบันทึก .env.local; รันเคส desktop application visual QA ที่สะดุดซ้ำผ่าน รวมยืนยันครบ 86 เคสที่เปิดใช้ ข้าม 4 เคสตามเงื่อนไขเดิม

`npm.cmd run test:safety` ผ่าน 5/5 และ `npm.cmd run build` ผ่าน การตรวจนี้ยืนยันการทำงานในเครื่องกับฐาน test ไม่ใช่ผลตรวจ API บน Vercel Preview ที่ต้องล็อกอิน

## ตรวจฟอร์มผู้จัดงาน อีเมลจริง และไฟล์แนบ — 13 กันยายน 2569

- `npm.cmd test` ผ่าน 108/108 กรณี ข้าม 6 กรณีโดยตั้งใจ (เทสที่เปลี่ยนข้อมูลจริงรันเฉพาะ desktop)
- `npm.cmd run test:auth` 9/9 · `npm.cmd run test:safety` 5/5 · `npm.cmd run build` ผ่าน
- axe ครบทั้งหน้า `/organizers` และฟอร์มทั้ง 4 ขั้น ไม่พบ violation

สิ่งที่เทสยืนยันเพิ่มในรอบนี้

- แต่ละขั้นของฟอร์มไม่ยอมไปต่อจนกว่าจะกรอกครบ และข้อความบอกชื่อสิ่งที่ขาด
- วันปิดรับต้องเป็นวันในอนาคต และไม่มีเงินรางวัลต้องบอกว่าผู้ชนะได้อะไรแทน
- ปุ่มแก้ไขจากหน้าตรวจทานพากลับไปขั้นที่ถูก ข้อมูลไม่หาย และติ๊กยืนยันความถูกต้องหลุดออก
- ใบที่ส่งพร้อมโปสเตอร์ไปโผล่ในคิวตรวจจริง และทีมตรวจเปิดไฟล์ได้
- อัปโหลดปฏิเสธชนิดไฟล์ที่ไม่รับและไฟล์เกิน 5 MB
- คนที่ยังไม่ล็อกอินกรอกฟอร์มได้แต่ส่งไม่ได้

บั๊กที่เจอและแก้ระหว่างตรวจ

- การ์ดในแถบแนะนำถูก flex บีบให้พอดีจอ ทำให้ไม่มีอะไรให้เลื่อน แก้ด้วย `flex: 0 0 290px`
- แถบเลื่อนวัดแล้วกินพื้นที่ 0px ตอนรันแบบ headless ซึ่งใช้แถบลอยเสมอ ต้องเปิดหน้าต่างจริงถึงจะวัดได้ว่าแถบแสดงจริง (ได้ 10px ตามที่ตั้งไว้)
- ใส่ `scrollbar-width` หรือ `scrollbar-color` คู่กับ `::-webkit-scrollbar` ทำให้เบราว์เซอร์ทิ้งกฎแล้วกลับไปใช้แถบลอย

ข้อจำกัดที่ยังอยู่

- อีเมลส่งได้จริงแล้วเมื่อมี `RESEND_API_KEY` แต่ที่อยู่ผู้ส่งเริ่มต้นส่งถึงได้เฉพาะเจ้าของบัญชี Resend ผู้จัดงานจะยังไม่ได้รับจนกว่าจะมีโดเมนของตัวเอง
- ใบสมัครเมนเทอร์ยังบันทึกหลักฐานเป็นชื่อไฟล์ ยังไม่ได้ต่อกับการอัปโหลดจริง

## ตรวจหน้ากรอกเวทีสำหรับทีมงาน — 13 กันยายน 2569

- `npm.cmd test` ผ่าน 119/119 กรณี ข้าม 10 กรณีโดยตั้งใจ
- `npm.cmd run test:auth` 9/9 · `npm.cmd run test:safety` 5/5 · `npm.cmd run build` ผ่าน
- axe ผ่านทั้งหน้ารายการเวทีและหน้าฟอร์ม

สิ่งที่เทสยืนยัน

- เฉพาะทีมตรวจเข้าถึง `/api/admin/listings` ได้ คนที่ยังไม่ล็อกอินได้ 401 สมาชิกทั่วไปได้ 403
- บันทึกไม่ได้ถ้าไม่มีลิงก์ประกาศต้นทาง หรือไม่มีเงินรางวัลแล้วไม่บอกว่าได้อะไรแทน
- เวทีที่เพิ่มจากหน้าจัดการขึ้นหน้าเว็บสาธารณะทันที เป็น `source: editorial`
- ส่วนที่กรอกบรรทัดละข้อกลายเป็นรายการบนหน้ารายละเอียด ส่วนที่เว้นว่างไม่ขึ้น
- แก้ข้อมูลแล้ว `lastVerifiedAt` ขยับเป็นวันนี้เอง
- ปุ่ม "ตรวจแล้ววันนี้" ลบคำเตือนข้อมูลค้างออกจากแถวนั้น

บั๊กที่เจอและแก้ระหว่างตรวจ

- `afterAll` ของ Playwright รันต่อ worker ไม่ใช่ต่อไฟล์ การล้างข้อมูลรวมทีเดียวใน hook นี้จึงลบของที่ worker อื่นกำลังใช้อยู่ ทำให้เทสล้มแบบสุ่ม แก้ด้วยการให้เทสเก็บของที่ตัวเองสร้าง
