# ChampionWays — หน้าจัดการสำหรับทีมงาน

สถานะ: ทำเป็นต้นแบบแล้วทั้งห้าหน้า เขียนและลงมือเมื่อ 12 กันยายน 2569

เอกสารนี้ออกแบบหน้าจัดการ (admin) ที่ทีมงานใช้ตรวจใบที่ส่งเข้ามาก่อนเผยแพร่ ใช้คู่กับ [organiser-submission.md](organiser-submission.md) ที่กำหนดเกณฑ์การตรวจ [competition-model.md](competition-model.md) ที่กำหนดโครงสร้างข้อมูล และ [design.md](design.md) ที่กำหนดหน้าตา

## 1. ข้อจำกัดที่ต้องรู้ก่อนอ่านต่อ

เว็บนี้เป็น static site ไม่มี backend และไม่มีระบบยืนยันตัวตน **หน้าจัดการที่อยู่ใน bundle เดียวกับหน้าบ้านจึงไม่มีระบบสิทธิ์จริง** ใครพิมพ์ `/admin` ก็เข้าได้ การใส่หน้า login ไว้จะยิ่งอันตรายเพราะทำให้เข้าใจผิดว่ามีการป้องกัน

รอบนี้จึงทำได้แค่ **ต้นแบบหน้าจอบนข้อมูลคิวตัวอย่าง** โดยต้องมีแถบเตือนบนทุกหน้า และ **ห้ามใส่ข้อมูลจริงของใครลงไป** จนกว่าจะมี backend กับ auth จริง การกดปุ่มตัดสินในต้นแบบไม่ถูกบันทึกไว้ที่ไหน

ที่ยังคุ้มจะออกแบบตอนนี้ เพราะหน้าตรวจเป็นตัวบอกว่าฟอร์มลงงานแข่งต้องเก็บอะไรบ้าง ถ้าทำฟอร์มก่อนแล้วค่อยทำหน้าตรวจ จะพบทีหลังว่าข้อมูลที่จำเป็นต่อการตัดสินใจไม่ได้ถูกถาม

## 2. หน้าและ route

| Route | หน้า | ตอบคำถามอะไร |
| --- | --- | --- |
| `/admin` | ภาพรวม | วันนี้ต้องทำอะไรก่อน |
| `/admin/competitions` | คิวงานแข่ง | มีใบไหนรออยู่ รอมานานแค่ไหน |
| `/admin/competitions/:id` | ตรวจงานแข่งรายใบ | ใบนี้ผ่านหรือไม่ผ่าน เพราะอะไร |
| `/admin/mentors` | คิวใบสมัครเมนเทอร์ | เหมือนกัน แต่มีข้อมูลส่วนบุคคล |
| `/admin/mentors/:id` | ตรวจเมนเทอร์รายใบ | ให้ป้าย "ยืนยันแล้ว" ได้ไหม |

ไม่มี `/admin/login` เพราะหน้า login ที่ป้องกันอะไรไม่ได้จริงสร้างความเข้าใจผิดมากกว่าประโยชน์

หน้าจัดการอยู่นอก `Layout` ของหน้าบ้าน เพราะไม่ควรมีปุ่มรายการที่บันทึกไว้ เมนูผู้ใช้ทั่วไป หรือ footer การตลาด แต่ยังใช้ theme tokens ชุดเดียวกันจาก [styles.css](src/styles.css)

## 3. หน้าภาพรวม

ตอบสี่คำถามให้ได้ในห้าวินาที ไม่ใช่หน้าสถิติ ตัวเลขที่ไม่นำไปสู่การลงมือทำห้ามอยู่ที่นี่

- **รอตรวจกี่ใบ และใบเก่าสุดรอมากี่วัน** — [organiser-submission.md](organiser-submission.md) ข้อ 6 ตั้งเป้าไว้ 2 วันทำการ เกินแล้วต้องขึ้นสถานะเตือน
- **เวทีที่ปิดรับใน 7 วัน** ซึ่งเป็นกลุ่มที่ต้องยืนยันกับผู้จัดก่อนถึงกำหนด
- **เวทีที่ทีมงานคัดมาเองและไม่ได้ตรวจเกิน 30 วัน** — ข้อมูลค้างทำลายความเชื่อถือเร็วกว่าข้อมูลน้อย
- **เวทีที่เลยวันปิดรับไปแล้ว** ซึ่งควรถูกย้ายออกจากรายการหลัก

ค่าที่เป็นศูนย์ให้แสดงว่า "ไม่มี" ไม่ใช่ซ่อนการ์ดทิ้ง เพราะการเห็นว่าตรวจแล้วไม่มีงานค้าง ก็เป็นข้อมูล

## 4. หน้าคิว

รายการเรียงตาม **รอนานสุดก่อน** เป็นค่าเริ่มต้น ไม่ใช่ใหม่สุดก่อน ไม่อย่างนั้นใบที่ตรวจยากจะถูกดองไปเรื่อย ๆ

แต่ละแถวบอก ชื่อ ผู้ส่ง วันที่ส่ง จำนวนวันที่รอ และสถานะ กรองตามสถานะได้: รอตรวจ / ขอข้อมูลเพิ่ม / เผยแพร่แล้ว / ไม่ผ่าน

## 5. หน้าตรวจรายใบ

สองคอลัมน์ ซ้ายคือใบที่ส่งมาทุก field ขวาคือรายการตรวจและปุ่มตัดสิน

**ปุ่ม "เผยแพร่" ต้องกดไม่ได้จนกว่าจะติ๊กรายการตรวจครบทุกข้อ** นี่คือหัวใจของหน้านี้ คือเอาเกณฑ์ที่เขียนไว้ในเอกสารมาบังคับด้วยระบบ แทนที่จะหวังว่าคนตรวจจะจำได้

รายการตรวจงานแข่ง ตาม [organiser-submission.md](organiser-submission.md) ข้อ 6:

1. ลิงก์ประกาศต้นทางเปิดได้จริง และเป็นประกาศของงานนี้
2. ชื่อหน่วยงานตรงกับที่ปรากฏในประกาศ
3. วันปิดรับยังไม่ผ่าน และตรงกับต้นทาง
4. เงินรางวัลและค่าสมัครตรงกับต้นทาง
5. คำบรรยายไม่ใช่ข้อความคัดลอกมาทั้งก้อนจากเว็บอื่น
6. ไม่ใช่การขายของหรือรับสมัครงานที่แฝงมาเป็นการแข่งขัน

รายการตรวจเมนเทอร์:

1. ตัวตนและที่ทำงานตรวจสอบได้จากข้อมูลที่ให้มา
2. หลักฐานรางวัลเป็นของผู้สมัครจริง และตรงกับเวทีที่อ้าง
3. ขอบเขต "ช่วยได้" และ "ช่วยไม่ได้" เขียนชัดและไม่ขัดกับกติกาของเวที
4. ราคาและคิวสมเหตุสมผล ไม่มีการชวนไปคุยนอกระบบ

ปุ่มตัดสินสามปุ่ม **เผยแพร่ / ขอข้อมูลเพิ่ม / ไม่ผ่าน** สองปุ่มหลังบังคับกรอกเหตุผล เพราะเอกสารสัญญาไว้ว่าจะแจ้งผลทุกกรณีพร้อมบอกวิธีแก้ ข้อความที่กรอกคือสิ่งที่ผู้ส่งจะได้อ่าน จึงต้องมีข้อความกำกับให้เขียนแบบที่คนอ่านแล้วแก้ต่อได้

หน้าตรวจงานแข่งต้องมี **พรีวิวการ์ดแบบที่จะขึ้นจริง** เพราะคนตรวจต้องเห็นสิ่งเดียวกับที่ผู้ใช้จะเห็น ไม่ใช่เห็นแค่ค่าในฟอร์ม และ `sourceUrl` ต้องเปิดในแท็บใหม่ได้ในคลิกเดียว

หน้าตรวจเมนเทอร์ต้อง **แยกด้วยสายตาให้ชัดว่าบล็อกไหนจะขึ้นสาธารณะ บล็อกไหนใช้ตรวจอย่างเดียว** เพราะฟอร์มสมัครสัญญากับผู้สมัครไว้แล้วว่าอีเมลและไฟล์หลักฐานใช้สำหรับตรวจสอบเท่านั้น หน้าจัดการต้องทำตามสัญญานั้น

การอนุมัติเมนเทอร์คือการตั้ง `wonSlug` และ `verified` ซึ่งเป็นตัวกำหนด tier 1 ในการจับคู่ ป้าย "ยืนยันแล้ว" ออกได้เมื่อตรวจหลักฐานจริงเท่านั้น ตาม [design.md](design.md) ข้อ 7

## 6. กฎร่วมของทุกหน้า

- เก็บร่องรอยการตรวจทุกครั้ง: ใครตรวจ เมื่อไหร่ เหตุผลอะไร (`reviewedBy` `reviewedAt` `reviewNote`)
- ไม่มีปุ่มลบถาวรในรอบนี้ ใช้สถานะ "ไม่ผ่าน" แล้วเก็บประวัติไว้
- ทุกหน้ามีแถบเตือนว่าเป็นต้นแบบ ไม่มีระบบสิทธิ์ และการตัดสินไม่ถูกบันทึก
- ใช้ tokens สี ตัวอักษร และระยะห่างจาก [styles.css](src/styles.css) เป้าแตะสูงอย่างน้อย 44px ตาม [design.md](design.md) ข้อ 6

## 7. สิ่งที่ยังไม่ทำรอบนี้

สิทธิ์หลายระดับ · แก้เนื้อหาเวทีจากหน้าจัดการ ซึ่งตอนนี้ยังแก้ในไฟล์ข้อมูล · สถิติและ analytics · การส่งอีเมลจริง · การจัดการผู้ใช้ · การอัปโหลดไฟล์จริง

## 8. สิ่งที่รู้หลังลงมือทำ

- **ปุ่มเผยแพร่ที่ถูกล็อกไว้ทำให้เกณฑ์การตรวจมีผลจริง** แต่ต้องปลดล็อกกลับด้วยเมื่อคนตรวจเอาติ๊กออก ไม่อย่างนั้นจะเผยแพร่ได้ทั้งที่เกณฑ์ไม่ครบ มีเทสคุมกรณีนี้ไว้
- การแยกบล็อก "ขึ้นหน้าเว็บ" กับ "ใช้ตรวจสอบเท่านั้น" ด้วยสีและไอคอนได้ผลกว่าการเขียนกำกับอย่างเดียว เทสตรวจตรงตัวว่าอีเมลกับนามสกุลเต็มต้องไม่อยู่ในบล็อกสาธารณะ
- ใบสมัครเมนเทอร์ที่อ้างรางวัลจากเวทีที่ไม่มีในระบบต้องขึ้นคำเตือนให้เห็น ไม่ใช่ปล่อยว่าง เพราะนั่นคือจุดที่ต้องตรวจด้วยมือ
- พรีวิวการ์ดใช้ `toCompetition()` แปลงใบเป็น `Competition` ซึ่งเป็นฟังก์ชันเดียวกับที่จะใช้ตอนเผยแพร่จริง ทำให้พรีวิวไม่มีวันหลุดจากของจริง
- หน้าจัดการอยู่ใน bundle เดียวกับหน้าบ้าน **ถ้า deploy ขึ้นโฮสต์สาธารณะ `/admin` จะเปิดได้จากอินเทอร์เน็ต** ตอนนี้ปลอดภัยเพราะข้อมูลเป็นตัวอย่างทั้งหมด แต่ห้ามใส่ข้อมูลจริงก่อนมี auth

## 9. อ้างอิงใน repo

- ชนิดข้อมูลของใบที่ส่งเข้ามาและคิวตัวอย่าง: [submissions.ts](src/data/submissions.ts)
- หน้าจัดการ: [src/pages/admin/](src/pages/admin/)
- สไตล์เฉพาะหน้าจัดการ: [admin.css](src/admin.css)
- เทส: [admin.spec.ts](tests/admin.spec.ts)
