# ChampionWays — Backend

สถานะ: ทำแล้วและใช้งานได้จริง เขียนและลงมือเมื่อ 12 กันยายน 2569

เอกสารนี้อธิบายฝั่งเซิร์ฟเวอร์ทั้งหมด ใช้คู่กับ [competition-model.md](competition-model.md) ที่ว่าด้วยโครงสร้างข้อมูล [admin-console.md](admin-console.md) ที่ว่าด้วยหน้าจัดการ และ [organiser-submission.md](organiser-submission.md) ที่ว่าด้วยเกณฑ์การตรวจ

## 1. สิ่งที่เปลี่ยนไปจากเดิม

ก่อนหน้านี้ทุกอย่างเป็นไฟล์นิ่ง ข้อมูลเวทีอยู่ในไฟล์ TypeScript และหน้าจัดการไม่มีระบบสิทธิ์ ตอนนี้

- ข้อมูลเวที เมนเทอร์ และใบที่ส่งเข้ามาอยู่ใน **Postgres** ไม่ใช่ไฟล์ในโปรเจกต์แล้ว
- มีระบบ **สมัครสมาชิก เข้าสู่ระบบด้วยอีเมล และเข้าสู่ระบบด้วย Google**
- หน้าจัดการ **ปิดจริง** ไม่ใช่แค่ซ่อนปุ่ม บัญชีทั่วไปเปิดเข้าไม่ได้ทั้งจากหน้าเว็บและจาก API
- การตัดสินของทีมตรวจ **ถูกบันทึกจริง** และใบที่ผ่านจะกลายเป็นเวทีบนหน้าเว็บทันที

**GitHub Pages ใช้ไม่ได้อีกต่อไป** เพราะเสิร์ฟไฟล์นิ่งอย่างเดียว รัน API ไม่ได้ workflow เดิมจึงถูกเปลี่ยนเป็นสั่งรันมือเท่านั้น ที่อยู่จริงของเว็บคือ Vercel ซึ่งรันทั้งหน้าเว็บและ `/api` ในโปรเจกต์เดียวกัน

## 2. สแต็ก

| ส่วน | ใช้อะไร | ทำไม |
| --- | --- | --- |
| HTTP | **Hono** | เล็ก ใช้ Web standard `Request`/`Response` จึงรันได้ทั้ง Node ตอน dev และ Vercel ตอน production ด้วยโค้ดชุดเดียว |
| ฐานข้อมูล | **Postgres** (Neon) | dev กับ production เป็นชนิดเดียวกัน ไม่มีปัญหา "รันในเครื่องผ่าน ขึ้นจริงพัง" |
| ORM | **Drizzle** | schema เป็น TypeScript ตัวเดียวกับ type ที่ใช้ในโค้ด และ migration เป็นไฟล์ SQL ที่อ่านออก |
| ตรวจข้อมูลเข้า | **zod** | ทุก endpoint ที่รับข้อมูลตรวจด้วย schema ก่อนเสมอ |
| รหัสผ่าน | **scrypt ของ Node** | ไม่มี native module ให้คอมไพล์พังบน Windows และทนการเดารหัสผ่านได้ดีพอ |

ไม่ได้ใช้ไลบรารี auth สำเร็จรูป เพราะ flow มีแค่สองอย่างคือรหัสผ่านกับ Google OAuth และการพึ่งไลบรารีสำหรับเรื่องความปลอดภัยแปลว่าต้องตามอัปเดตมันตลอด

## 3. โครงไฟล์

```
server/
  app.ts            รวม route ทั้งหมด จัดการ error กลาง
  index.ts          เซิร์ฟเวอร์ Node สำหรับ dev
  db/
    schema.ts       16 ตาราง 10 enum
    client.ts       การเชื่อมต่อ
    queries.ts      คิวรีรายการเวทีและตัวกรอง
    migrations/     ไฟล์ SQL ที่ drizzle-kit สร้าง
    seed.ts         ใส่ข้อมูลตัวอย่างจาก src/data
    create-admin.ts สร้างบัญชีทีมตรวจ
  lib/
    env.ts          อ่าน .env.local ด้วย loader ของ Node เอง
    id.ts           id, token, slug
    password.ts     scrypt
    session.ts      session ในฐานข้อมูล + คุกกี้
    google.ts       OAuth ด้วย fetch ธรรมดา
    guards.ts       middleware ตรวจสิทธิ์
    files.ts        adapter ไฟล์ (ตอนนี้ลง disk)
    email.ts        adapter อีเมล (ตอนนี้ลงตาราง)
  routes/
    auth.ts         สมัคร เข้าสู่ระบบ ออกจากระบบ Google
    public.ts       รายการเวที รายละเอียด เมนเทอร์ รับใบที่ส่งเข้ามา
    admin.ts        ภาพรวม คิว และการตัดสิน
api/index.ts        ทางเข้าเดียวของ API บน Vercel
```

## 4. ระบบบัญชีและสิทธิ์

**ตารางผู้ใช้มีตารางเดียว** ไม่แยกตามบทบาท ผู้จัดงานกับผู้สมัครเมนเทอร์คือสมาชิกที่ส่งใบเข้ามา บทบาทมีสามระดับ

- `member` — ค่าเริ่มต้นของทุกคนที่สมัคร ส่งใบลงงานแข่งและใบสมัครเมนเทอร์ได้
- `reviewer` — เข้าหน้าจัดการและตัดสินใบได้
- `admin` — เหมือน reviewer เผื่อไว้สำหรับสิทธิ์ที่มากกว่าในอนาคต

**สองบทบาทหลังตั้งจากฝั่งเซิร์ฟเวอร์เท่านั้น** ด้วย `npm run db:admin` ไม่มีทางสมัครเอาเองจากหน้าเว็บ และสคริปต์นั้นไม่มีรหัสผ่านตั้งต้นให้ เพราะรหัสผ่านตั้งต้นคือช่องโหว่ทันทีที่ deploy

**Session อยู่ในฐานข้อมูล** ไม่ใช่ JWT เพราะต้องการยกเลิกได้ทันที คุกกี้เป็น `httpOnly` (JavaScript ในหน้าอ่านไม่ได้) `sameSite=Lax` (กัน CSRF ข้ามเว็บ) และ `secure` เมื่ออยู่ production

**Google OAuth** ใช้ Authorization Code พร้อม PKCE และ `state` โดยเก็บ state กับ verifier ไว้ในคุกกี้อายุสั้น กฎสำคัญสองข้อ

- อีเมลที่ Google ยัง**ไม่ได้ยืนยัน** ใช้ผูกบัญชีไม่ได้ ไม่อย่างนั้นจะสวมรอยบัญชีเดิมที่ใช้อีเมลเดียวกันได้
- ผูกบัญชีด้วย `sub` ของ Google ไม่ใช่อีเมล เพราะอีเมลเปลี่ยนได้

ถ้าไม่ได้ตั้ง `GOOGLE_CLIENT_ID` และ `GOOGLE_CLIENT_SECRET` ปุ่มเข้าสู่ระบบด้วย Google จะ**ไม่ขึ้นเลย** ดีกว่าขึ้นแล้วกดไม่ได้

## 5. Endpoints

ทุกเส้นทางขึ้นต้นด้วย `/api`

| Method | Path | สิทธิ์ | ทำอะไร |
| --- | --- | --- | --- |
| GET | `/health` | เปิด | ตรวจว่าเซิร์ฟเวอร์ยังตอบ |
| GET | `/auth/providers` | เปิด | บอกว่าเปิด Google ไว้ไหม |
| GET | `/auth/me` | เปิด | ผู้ใช้ปัจจุบันหรือ null |
| POST | `/auth/signup` | เปิด | สมัครด้วยอีเมล |
| POST | `/auth/login` | เปิด | เข้าสู่ระบบ |
| POST | `/auth/logout` | ล็อกอิน | ออกจากระบบ |
| GET | `/auth/google` | เปิด | พาไปหน้า Google |
| GET | `/auth/google/callback` | เปิด | รับกลับจาก Google |
| GET | `/competitions` | เปิด | รายการเวที รับตัวกรองทุกกลุ่มและแบ่งหน้า |
| GET | `/competitions/options` | เปิด | ชื่อเวทีทั้งหมดสำหรับ dropdown |
| GET | `/competitions/:slug` | เปิด | รายละเอียด พร้อมเวทีที่เกี่ยวข้อง |
| GET | `/mentors` | เปิด | เมนเทอร์ที่เผยแพร่แล้ว |
| POST | `/submissions/competition` | ล็อกอิน | ผู้จัดส่งงานแข่งเข้ามา |
| POST | `/submissions/mentor` | ล็อกอิน | ส่งใบสมัครเมนเทอร์ |
| GET | `/submissions/mine` | ล็อกอิน | ใบที่ตัวเองส่งไว้ |
| GET | `/admin/overview` | reviewer | สี่ตัวเลขบนหน้าภาพรวม |
| GET | `/admin/competition-submissions` | reviewer | คิวงานแข่ง กรองตามสถานะได้ |
| GET | `/admin/competition-submissions/:id` | reviewer | ใบเดียวพร้อมรายการตรวจและร่องรอย |
| POST | `/admin/competition-submissions/:id/decision` | reviewer | เผยแพร่ / ขอข้อมูลเพิ่ม / ไม่ผ่าน |
| GET | `/admin/mentor-submissions` | reviewer | คิวเมนเทอร์ |
| GET | `/admin/mentor-submissions/:id` | reviewer | ใบเดียว พร้อมบอกว่ารางวัลตรงกับเวทีในระบบไหม |
| POST | `/admin/mentor-submissions/:id/decision` | reviewer | ตัดสินใบสมัครเมนเทอร์ |

## 6. กฎที่เซิร์ฟเวอร์บังคับเอง

หน้าเว็บถูกข้ามได้เสมอ กฎเหล่านี้จึงบังคับซ้ำที่เซิร์ฟเวอร์ ไม่ใช่พึ่งการซ่อนปุ่ม

- **เผยแพร่ไม่ได้ถ้ารายการตรวจไม่ครบ** ส่ง `checks` ไม่ครบมาจะได้ 422 พร้อมบอกว่าเหลืออีกกี่ข้อ
- **ไม่ผ่านหรือขอข้อมูลเพิ่มต้องมีเหตุผล** ส่ง `note` ว่างมาจะได้ 422 เพราะข้อความนั้นคือสิ่งที่ผู้ส่งจะได้อ่าน
- **ใบที่เผยแพร่แล้วเผยแพร่ซ้ำไม่ได้** จะได้ 409 ไม่ใช่สร้างเวทีซ้ำ
- **หน้าจัดการต้องเป็น reviewer หรือ admin** สมาชิกทั่วไปได้ 403
- **ข้อมูลติดต่อของผู้จัดและอีเมลของผู้สมัครไม่เคยออกทาง API สาธารณะ** อยู่เฉพาะใน `/admin/*`
- **ป้าย "ยืนยันแล้ว" ของเมนเทอร์ออกให้เองไม่ได้** ระบบตั้งให้เมื่อมีรางวัลที่ตรงกับเวทีในระบบเท่านั้น
- **ลบบัญชีที่เคยตรวจใบไม่ได้** foreign key กันไว้ เพราะร่องรอยการตรวจต้องอยู่ต่อแม้คนตรวจจะออกไปแล้ว

## 7. ไฟล์และอีเมล

ทั้งสองอย่างทำเป็น adapter ไว้ เปลี่ยนที่เดียวโดยที่ผู้เรียกไม่ต้องแก้

- **ไฟล์** ตอนนี้เขียนลงโฟลเดอร์ `server/uploads` และบันทึกแถวในตาราง `files` ตอนย้ายไป S3 หรือ R2 ให้แก้แค่ `server/lib/files.ts` โครงตารางไม่ต้องแก้เพราะ `path` เก็บได้ทั้งชื่อไฟล์และ object key
- **อีเมล** ตอนนี้ยังไม่ส่งจริง บันทึกลงตาราง `email_log` แทน เพื่อให้ตรวจได้ว่าระบบจะส่งอะไรออกไป ตอนต่อ Resend หรือ SES ให้แก้แค่ `server/lib/email.ts`

## 8. คำสั่งและตัวแปรแวดล้อม

```
npm run dev        หน้าเว็บที่ 5173 (proxy /api ไป 8787 ให้เอง)
npm run dev:api    API ที่ 8787 แบบ watch
npm run db:generate  สร้างไฟล์ migration จาก schema
npm run db:migrate   รัน migration กับฐานข้อมูลจริง
npm run db:seed      ล้างและใส่ข้อมูลตัวอย่างใหม่
npm run db:admin     สร้างหรืออัปเกรดบัญชีทีมตรวจ
npm test             เทสทั้งหมด (seed ใหม่ก่อนรันทุกครั้ง)
```

ตัวแปรทั้งหมดอยู่ใน [.env.example](.env.example) ค่าจริงอยู่ใน `.env.local` ซึ่ง `.gitignore` ครอบไว้แล้ว **ห้าม commit และห้ามวางลงในแชต**

ตอน deploy ขึ้น Vercel ต้องตั้ง `DATABASE_URL` และ `APP_ORIGIN` (เป็นที่อยู่จริงของเว็บ) ใน Environment Variables ของโปรเจกต์ และถ้าใช้ Google ต้องตั้ง `GOOGLE_CLIENT_ID` `GOOGLE_CLIENT_SECRET` พร้อมเพิ่ม `<APP_ORIGIN>/api/auth/google/callback` เข้าไปใน Authorized redirect URIs

## 9. สิ่งที่รู้หลังลงมือทำ

- **`sql\`... in ${array}\`` ผูกค่าผิด** drizzle จะมองอาเรย์เป็นพารามิเตอร์ตัวเดียว ต้องใช้ `inArray()` หรือ `exists()` กับ subquery จริง
- **ใช้ `exists` ไม่ใช่ `join` เวลากรองด้วยตารางลูก** เพราะเวทีหนึ่งมีหลายหมวด ถ้า join แถวจะซ้ำแล้วนับผิด
- **สระและวรรณยุกต์ไทยเป็น Unicode Mark ไม่ใช่ Letter** regex ที่เก็บแค่ `\p{L}` จะตัดทิ้งจนได้ slug อย่าง `หน-งส-นสามนาท`
- **เรียก `reload()` หลังตัดสินทำให้ component ถูกถอดทิ้ง** ข้อความยืนยันผลหายไปทันที ต้องเช็ก `loading && !data` แทน `loading` เฉย ๆ
- **ข้อความ error จาก zod ต้องบอกชื่อฟิลด์ด้วย** "expected string, received undefined" อย่างเดียวทำให้ทั้งผู้กรอกและคนแก้บั๊กต้องเดา
- **เซิร์ฟเวอร์ที่ค้างอยู่คือกับดัก** ตอนหา bug หนึ่งรอบ ปรากฏว่าแก้โค้ดแล้วแต่ API ที่รันอยู่เป็นตัวเก่า ถ้ารัน `npm run dev:api` แบบ watch จะไม่เจอปัญหานี้

## 10. ที่ยังไม่ได้ทำ

- **ฟอร์มให้ผู้จัดลงงานแข่งบนหน้าเว็บ** API รับได้แล้ว (`POST /api/submissions/competition`) แต่ยังไม่มีหน้าให้กรอก ตอนนี้ต้องยิง API ตรง แบบที่เทสทำ ดีไซน์อยู่ใน [organiser-submission.md](organiser-submission.md) ข้อ 5
- **อัปโหลดไฟล์จริงจากหน้าเว็บ** adapter พร้อมแล้วแต่ฟอร์มยังไม่ได้ส่งไฟล์ขึ้นไป บันทึกไว้เป็นชื่อไฟล์ให้ทีมตรวจขอเพิ่มภายหลัง
- **ส่งอีเมลจริง** ยังบันทึกลงตารางอย่างเดียว
- **ยืนยันอีเมลตอนสมัคร** และรีเซ็ตรหัสผ่าน
- **จำกัดจำนวนคำขอต่อไอพี** ตาม [organiser-submission.md](organiser-submission.md) ข้อ 7
- **หน้าให้ผู้ใช้ดูใบที่ตัวเองส่ง** API มีแล้ว (`GET /api/submissions/mine`) แต่ยังไม่มีหน้า
