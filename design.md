# ChampionWays — แนวทางดีไซน์หลัก

สถานะ: แนวทางหลักสำหรับสร้างและแก้ไข UI ของโปรเจกต์ ตั้งแต่ 12 กันยายน 2569

เอกสารนี้ยึดเว็บเวอร์ชันปัจจุบัน ได้แก่ หน้าสำรวจ หน้าเมนเทอร์ และหน้าสมัครเมนเทอร์ 4 ขั้นตอน เป็นฐาน การพัฒนาหน้าใหม่ให้ใช้แนวทางนี้ร่วมกับ components และ CSS variables ที่มีอยู่ หากมีคำสั่งใหม่จากเจ้าของโปรเจกต์ให้ปรับแนวทาง ให้แก้เอกสารนี้ให้ตรงกับการตัดสินใจด้วย

ธีมหลักคือ **ม่วง–ทอง พื้นสว่าง อบอุ่น อ่านง่าย และแสดงข้อมูลอย่างตรงไปตรงมา** ใช้แทนแนวทางสีน้ำเงิน cobalt และรายการแถวจากภาพอ้างอิงแรก สีและสัดส่วนจาก HTML ที่นำเข้าเป็นแหล่งอ้างอิงการออกแบบ ไม่ใช่ข้อกำหนดให้คัดลอกโค้ดหรือพฤติกรรมทั้งหมด

## 1. วิเคราะห์บุคลิกและโครงสร้าง

ChampionWays ช่วยนักเรียนและนักศึกษาค้นหาเวทีแข่งขัน และเลือกคนที่ช่วยเตรียมตัวได้ บุคลิกควรเหมือนรุ่นพี่ที่อธิบายชัดเจน: เป็นมิตร มีความหวัง และมีหลักฐานรองรับสิ่งที่กล่าวอ้าง

| สิ่งที่เห็นในเว็บ | ผลต่อประสบการณ์ | แนวทางที่ใช้ต่อ |
| --- | --- | --- |
| ม่วงเข้มสำหรับปุ่มและข้อความสำคัญ | เห็นสิ่งที่กดได้และจำแบรนด์ได้ | ใช้ม่วงกับ action และสถานะที่เลือกเป็นหลัก |
| ม่วงอ่อนผสมเหลืองอุ่นใน hero | สื่อถึงโอกาสและการเติบโต | จำกัด gradient ไว้ในพื้นที่แนะนำ ภาพปก และไฮไลต์ |
| การ์ดขาว เส้นขอบบาง เงาฟุ้งเบา | แยกกลุ่มข้อมูลโดยไม่ทำให้หน้าหนัก | ใช้เส้นขอบเป็นฐาน ใช้เงาช่วยยกพื้นที่สำคัญ |
| ถ้วยรางวัลและรูปทรงเรขาคณิต | เชื่อมภาพจำกับการแข่งขัน | ใช้ artwork เดิมและภาพ SVG ที่มีภาษาภาพร่วมกัน |
| ฟอร์มเมนเทอร์ถามบริบทก่อนแสดงตัวเลือก | ช่วยให้เลือกจากความเหมาะสม | เรียงคำถามตามสิ่งที่ผู้ใช้ต้องตัดสินใจ |
| ใบสมัครแบ่ง 4 ขั้นตอนและมี preview | ลดข้อมูลที่ต้องจัดการพร้อมกัน | บอกขั้นตอน ข้อมูลจำเป็น และผลของการกดปุ่มเสมอ |

จุดที่ต้องรักษาสมดุล: หน้าแรกมี hero และไฮไลต์หลายส่วนก่อนถึงการค้นหา จึงไม่ควรเพิ่มส่วนตกแต่งเหนือช่องค้นหาอีก การจัดอันดับช่วยดึงสายตาได้ แต่ข้อมูลทดลองต้องระบุว่าเป็นอันดับตัวอย่าง ความน่าเชื่อถือควรมาจากรายละเอียด ขอบเขต และสถานะที่ตรวจสอบได้

## 2. สีและการใช้งาน

ค่าต่อไปนี้มาจาก `:root` ใน [src/styles.css](src/styles.css) ให้ใช้ `var(...)` แทนการใส่ hex ซ้ำใน components ใหม่

| Token | ค่า | บทบาท |
| --- | --- | --- |
| `--paper` | `#FCFBFE` | พื้นหลังทั้งหน้า |
| `--card` | `#FFFFFF` | การ์ด ฟอร์ม และ header |
| `--ink` | `#1C1917` | ข้อความหลัก |
| `--ink-deep` | `#21172D` | หัวข้อที่ต้องการน้ำหนัก |
| `--muted` | `#6B625C` | คำอธิบายรอง |
| `--muted-soft` | `#756C83` | metadata และข้อความช่วย |
| `--line` | `#EAE5F0` | เส้นแบ่งและขอบการ์ด |
| `--line-warm` | `#F1EAE4` | เส้นแบ่งบนพื้นโทนอุ่น |
| `--purple` | `#6D28D9` | สีหลักของแบรนด์ |
| `--purple-bright` | `#7C3AED` | ปลาย gradient ของปุ่มหลัก |
| `--purple-deep` | `#5B21B6` | ลิงก์และข้อความ action |
| `--purple-tint` | `#F7F1FF` | พื้นตัวเลือกที่เลือกและพื้นรอง |
| `--purple-soft` | `#7F5AAC` | เลขส่วนและองค์ประกอบรอง |
| `--gold` | `#F5D95C` | รางวัลและจุดเน้นเล็ก ๆ |
| `--gold-tint` | `#FFF9E5` | กล่องหมายเหตุ |
| `--peach` | `#FDF1EA` | พื้นป้ายหมวดและตัวกรองบางชนิด |
| `--peach-line` | `#F3D3C0` | ขอบตัวกรองโทนพีช |

- ปุ่มหลักใช้ `--brand-gradient`: `linear-gradient(135deg, #6D28D9, #7C3AED)` และตัวอักษรขาว
- Hero ใช้ม่วงอ่อนด้านซ้าย ไล่สู่พื้นขาวและเหลืองอ่อนด้านขวา พื้นเนื้อหาและช่องกรอกใช้สีเรียบ
- ทองใช้กับเหรียญ จุดเน้น หรือพื้นหมายเหตุ ไม่ใช้ตัวอักษรทองอ่อนบนพื้นขาว
- ข้อผิดพลาดปัจจุบันใช้ `#A3341F` ในฟอร์มเมนเทอร์ และ `#A43148` ในใบสมัคร ต้องมีข้อความบอกวิธีแก้ร่วมด้วย
- การตรวจ contrast ต้องอิงคู่สีและพื้นหลังจริง โดยเฉพาะข้อความรองบน gradient ห้ามถือว่า token ผ่านทุกบริบทโดยอัตโนมัติ

## 3. ตัวอักษรและภาษาไทย

ใช้ฟอนต์ที่เก็บในโปรเจกต์: `Anuphan` สำหรับหัวข้อ ผ่าน `--display`; `IBM Plex Sans Thai` และ `IBM Plex Sans` สำหรับเนื้อหา ผ่าน `--body`

| บทบาท | แนวทางขนาด | น้ำหนัก / ระยะบรรทัด |
| --- | --- | --- |
| H1 หน้าภายใน | desktop 38–40px, mobile 28–32px | 600–700 / 1.3 |
| H2 หัวข้อส่วน | 21–26px | 600 / ประมาณ 1.4 |
| H3 ชื่อการ์ด | 19–20px | 600 / 1.35–1.5 |
| เนื้อหาทั่วไป | 14px; คำอธิบายยาว 15–16px | 400 / 1.65–1.85 |
| ปุ่มและ label | 14–15px | 500–600 / อย่างน้อย 1.5 |
| metadata | 12–13px | 400–500 / อย่างน้อย 1.6 |

เว็บเดิมมีข้อความเล็กบางตำแหน่งที่ 11–11.5px ให้ใช้เฉพาะข้อมูลประกอบที่ไม่จำเป็นต่อการตัดสินใจ งานใหม่ไม่ใช้ขนาดนี้กับคำสั่งหรือข้อผิดพลาด

หัวข้อใช้ letter spacing เล็กน้อยตาม stylesheet เดิม ห้ามบีบตัวอักษรไทยเพิ่มหรือกำหนดความสูงตายตัวจนสระและวรรณยุกต์ถูกตัด ชื่อเวทีและข้อความสำคัญให้ขึ้นบรรทัดได้ URL และชื่อไฟล์ยาวใช้ `overflow-wrap: anywhere` ส่วนข้อความปุ่มสั้นควรอยู่บรรทัดเดียว

ใช้ภาษาไทยตรงประเด็น เช่น “ดูรายละเอียด”, “ย้อนกลับ”, “ถัดไป”, “ส่งใบสมัครตัวอย่าง” ข้อความผิดพลาดระบุสิ่งที่ต้องแก้ เช่น “เลือกความถนัดให้ครบ 2 หัวข้อ” หลีกเลี่ยงคำยืนยันผลสำเร็จที่ระบบยังไม่ได้ทำจริง

## 4. Layout ระยะห่าง และพื้นผิว

- Container หลัก `.shell`: max-width 1180px รวม padding ซ้ายขวา 28px; ที่หน้าจอไม่เกิน 700px ใช้ 18px
- ใช้ระยะห่างหลัก 4 / 8 / 12 / 16 / 24 / 32 / 48 / 72px งานเดิมมี 18 / 20 / 22 / 26 / 28px ตาม component ให้ใช้ของเดิมเมื่อขยาย component เดียวกัน
- ระหว่างข้อมูลที่สัมพันธ์กันใช้ 8–16px ระหว่างกลุ่มข้อมูล 24–32px ระหว่างส่วนใหญ่ 40–72px
- มุมโค้ง controls 8–12px, การ์ด 16–20px, พื้นที่เด่น 20–24px; pill ใช้ 999px เฉพาะตัวเลือกหมวดหรือป้ายที่เหมาะสม
- ใช้ขอบ 1px สี `--line` และเงา `--shadow-card`; hover ของการ์ดที่กดได้ใช้ `--shadow-lift` ได้ ไม่ใส่เงาหนักให้ทุกกล่อง
- เนื้อหายาวและขั้นตอนใช้เส้นแบ่ง แทนการซ้อนการ์ดหลายชั้น
- รักษาขอบซ้ายของหัวข้อ ช่องกรอก ข้อความช่วย และปุ่มให้สัมพันธ์กัน

## 5. Components หลัก

### Header และ footer

ใช้ [Layout.tsx](src/components/Layout.tsx) ร่วมกันทุกหน้า มีเส้นม่วง–ทองสูง 3px บนสุด โลโก้ถ้วยและชื่อแบรนด์ เมนูหลัก และ action ด้านขวา Active navigation ใช้พื้นอุ่นอ่อนตามเว็บปัจจุบัน ไม่ต้องใช้ปุ่มม่วงทุกตำแหน่งที่เลือก

ใช้ `src/assets/trophy.png` สำหรับเครื่องหมาย และ `src/assets/wordmark.jpg` สำหรับ artwork ใน hero รักษาสัดส่วน ไม่ยืดภาพหรือสร้างโลโก้ใหม่เอง ภาพที่มีชื่อแบรนด์ข้างกันใช้ alt ว่างเพื่อไม่อ่านซ้ำ

Footer ใช้พื้นอ่อน เส้นแบ่ง และลิงก์ที่ใช้งานได้ ส่วนที่ยังไม่รองรับต้องแสดงให้รู้ว่าไม่ใช่ลิงก์พร้อมใช้งาน

### ปุ่มและลิงก์

- Primary: action หลักของกลุ่ม ใช้ม่วง ตัวอักษรขาว และเงาเบา
- Secondary: พื้นขาว ขอบอ่อน ตัวอักษรม่วง ใช้ย้อนกลับ หรือตัวเลือกเสริม
- Text link: ใช้นำทางหรือ action รองที่ไม่ต้องแย่งสายตา; ลิงก์ในข้อความต้องแยกจากเนื้อหาได้
- ใช้ `<Link>` สำหรับเปลี่ยนหน้า และ `<button>` สำหรับกระทำในหน้า ห้ามทำ div ที่คลิกได้แทน
- แสดง hover, focus, selected และ disabled ชัดเจน ไม่ใช้สีเพียงอย่างเดียวบอกสถานะ
- Controls ใหม่ที่ใช้สัมผัสควรมีพื้นที่กดอย่างน้อย 44×44px; controls เดิมบางตัวเล็กกว่านี้ เป็นงานปรับปรุงต่อ ไม่ใช่ข้ออ้างว่าทั้งเว็บผ่านเกณฑ์นี้แล้ว

### การ์ดการแข่งขัน

ใช้ภาพปก SVG 16:9 ที่ไล่สีตามหมวดหลัก ตามด้วยหมวด ผู้จัด ชื่อ คำอธิบาย ข้อมูลตัดสินใจ และ action ด้านล่าง ชื่อและคำอธิบายยืดความสูงได้ ใช้ flex จัดข้อมูลและปุ่มให้สม่ำเสมอ

หนึ่งงานอยู่ได้หลายหมวด การ์ดจึงแสดงชื่อหมวดหลักเต็ม แล้วต่อด้วยป้าย `+N` สำหรับหมวดที่เหลือ ป้ายนี้ต้องมี `aria-label` ที่บอกชื่อหมวดที่เหลือครบ ไม่ปล่อยให้ผู้อ่านเดา เงินรางวัลคำนวณป้ายจากตัวเลขเสมอ และค่า 0 ต้องอ่านว่า “ไม่มีเงินรางวัล” หรือชื่อรางวัลที่ไม่ใช่เงิน ไม่ใช่ “0 บาท”

หน้ารายละเอียดมีกล่อง “ที่มาของข้อมูล” ใต้กล่องสรุป บอกว่าข้อมูลมาจากผู้จัดเองหรือทีมงานคัดมา วันที่ตรวจล่าสุด และลิงก์ประกาศต้นทาง เวทีที่เป็นข้อมูลตัวอย่างให้บอกตรง ๆ ว่ายังไม่มีประกาศต้นทางให้เปิด

Desktop ใช้ grid ที่มีความกว้างขั้นต่ำประมาณ 300px ช่องว่าง 24px และลดจำนวนคอลัมน์ตามพื้นที่ ปุ่มบันทึกต้องมีสถานะที่อ่านได้ ไม่ใช้ hover เป็นทางเดียวในการพบ action

### ชิปหมวดและแผงตัวกรอง

ชิปหมวด 13 หมวดพร้อมชิป “ทั้งหมด” อยู่ใต้ช่องค้นหา **เลือกได้หลายหมวดพร้อมกัน** และกดซ้ำเพื่อเอาออก ทุกชิปเป็นปุ่มที่มี `aria-pressed` บอกสถานะ ชิป “ทั้งหมด” ถือว่าถูกเลือกเมื่อยังไม่ได้เลือกหมวดใดเลย และกดแล้วล้างหมวดทั้งหมด

ปุ่ม “ตัวกรอง” อยู่ท้ายแถวค้นหา มีตัวเลขบอกจำนวนตัวกรองที่เปิดอยู่ นับเฉพาะตัวกรองในแผง ไม่นับหมวดกับคำค้นซึ่งเห็นได้จากหน้าอยู่แล้ว

แผงตัวกรองใช้ `<dialog>` แบบ modal เพื่อให้ได้ focus trap ปุ่ม Esc และการปิดฉากหลังจากเบราว์เซอร์ ไม่ต้องเขียนเอง เปิดเป็น drawer กว้าง 420px จากซ้ายบน desktop และเต็มจอเมื่อไม่เกิน 700px ส่วนหัวมีชื่อกับปุ่มปิดขนาด 44px ส่วนท้ายตรึงอยู่กับที่ มีปุ่ม “ล้างทั้งหมด” และ “ดูผลลัพธ์ N รายการ” ที่ N อัปเดตสดระหว่างเลือก

แต่ละกลุ่มในแผงเป็น `fieldset` พร้อม `legend` ไม่ใช่ `div` กับข้อความหนา คั่นกลุ่มด้วยเส้นแบ่งแบบเดียวกับ `.group` ในใบสมัคร ตัวเลือกเป็นกล่องกดได้ทั้งแถวสูงอย่างน้อย 44px และบอกสถานะเลือกด้วยพื้นกับขอบ ไม่ใช่สีอย่างเดียว

ตัวกรองทุกตัวเก็บไว้ที่ query string เพื่อให้แชร์ลิงก์และกดย้อนกลับได้ รายละเอียดคีย์และกฎการรวมเงื่อนไขอยู่ใน [competition-model.md](competition-model.md)

### Highlights และเมนเทอร์

Highlights ใช้การ์ดขนาดกระชับและอันดับที่อ่านง่าย แถวนี้ **ไม่เลื่อนเอง** ผู้ใช้เลื่อนเองด้วยแถบเลื่อนที่เห็นตลอดด้านล่าง จึงไม่ต้องมีปุ่มหยุดกำกับตามข้อกำหนดเรื่องเนื้อหาที่ขยับเอง กล่องที่เลื่อนได้ต้องรับ focus ได้ (`tabIndex={0}`) พร้อมชื่อกำกับ เพื่อให้เลื่อนด้วยลูกศรบนคีย์บอร์ดได้ และการ์ดต้องไม่หด (`flex: 0 0 <ความกว้าง>`) ไม่อย่างนั้นจะถูกบีบให้พอดีจอจนไม่มีอะไรให้เลื่อน

แถบเลื่อนใช้ `::-webkit-scrollbar` ล้วน ห้ามใส่ `scrollbar-width` หรือ `scrollbar-color` คู่กัน เพราะเบราว์เซอร์จะทิ้งกฎแล้วกลับไปใช้แถบลอยที่โผล่เฉพาะตอนเลื่อน หมายเหตุ: เบราว์เซอร์แบบ headless ใช้แถบลอยเสมอ การวัดว่าแถบแสดงจริงจึงต้องเปิดหน้าต่างจริง

Podium บน desktop วางอันดับ 1 กลางและเด่นกว่าเล็กน้อย บนมือถือเรียง 1 → 2 → 3 ข้อมูลอันดับสมมติต้องระบุว่าเป็นตัวอย่าง

หน้าเมนเทอร์เริ่มจากบริบทของทีม แล้วจึงแสดงเมนเทอร์ที่เหมาะสม ใช้ summary ที่เปิดดูรายละเอียดได้ ภายในแยกประสบการณ์ หลักฐาน ช่วยได้/ช่วยไม่ได้ ความถนัด และข้อมูลคิว ราคาให้บอกหน่วยต่อทีม ระยะเวลา และการหารต่อคนชัดเจน

### ฟอร์มและใบสมัครเมนเทอร์

ใช้ label เหนือช่องกรอก พื้นขาว ขอบม่วงอ่อน มุม 8–9px และ padding ประมาณ 11px 12px มีข้อความช่วยใต้ field; placeholder เป็นเพียงตัวอย่าง ไม่ใช้แทน label

ใบสมัครอยู่ที่ `/mentors/apply` ใช้ 4 ขั้นตอนตามลำดับ:

1. ข้อมูลผู้สมัคร — ตัวตน ช่องทางติดต่อ และภาพโปรไฟล์
2. ประสบการณ์และรางวัล — เรียงประสบการณ์ → หลักฐานรางวัลสูงสุด 2 งาน → ผลงานหรือ Portfolio
3. บริการและคิว — ขอบเขตการช่วย ความถนัด ราคา และคิวที่ไม่ทับกัน
4. ตรวจทานและส่ง — preview ข้อมูลสาธารณะ แยกข้อมูลส่วนตัว และยืนยันก่อนส่งตัวอย่าง

ขั้นตอนที่ยาวให้แบ่งเป็นกลุ่มด้วย `.group` ซึ่งใช้เส้นแบ่งคั่น ไม่ซ้อนการ์ด แต่ละกลุ่มมีหัวข้อ h3 คำอธิบายสั้นหนึ่งบรรทัด และช่องนับหรือปุ่มประกอบทางขวาของหัวข้อเมื่อจำเป็น

ตัวเลือกที่กดได้ทั้งแถว เช่น ความถนัดและช่องยอมรับเงื่อนไข ใช้กล่องสูงอย่างน้อย 44px มีสถานะเลือกด้วยพื้นและขอบ ไม่ใช้สีอย่างเดียว ค่าที่คำนวณจากสิ่งที่กรอก เช่น ราคาหารต่อคน ให้วางติดกับช่องกรอกต้นทาง เพื่อให้อ่านคู่กันได้ทันที

หน้าตรวจทานเริ่มด้วยตัวอย่างโปรไฟล์ที่ทีมจะเห็น แล้วจึงเรียงข้อมูลที่ใช้พิจารณาเป็นกลุ่ม แต่ละกลุ่มมีปุ่ม “แก้ไข” ที่มีชื่อเฉพาะกลุ่ม เช่น “แก้ไขบริการและราคา” กลับไปขั้นที่เกี่ยวข้องโดยข้อมูลและไฟล์ยังอยู่ครบ

ความยินยอมและเงื่อนไขอยู่ท้ายหน้าตรวจทาน ทุกช่องเริ่มต้นไม่ถูกเลือกและบังคับติ๊กครบ แยกเงื่อนไขการเป็นเมนเทอร์ออกจากข้อรับทราบเรื่องการโอนเงินด้วยเส้นแบ่ง กดส่งขณะติ๊กไม่ครบให้แสดงข้อความและย้าย focus ไปช่องแรกที่ขาด และเมื่อผู้สมัครกลับไปแก้ข้อมูลใบสมัคร ให้ล้างเฉพาะข้อยืนยันความถูกต้องของข้อมูล เพื่อบังคับให้ตรวจทานใหม่

Desktop ใช้ sidebar 230px กับฟอร์มหลัก ช่องว่าง 36px ใน container สูงสุด 1150px; ที่ไม่เกิน 1000px ย้ายตัวบอกขั้นตอนไว้เหนือฟอร์มและเรียง field คอลัมน์เดียว ที่ไม่เกิน 480px ย้ายจำนวนขั้นตอนไปคนละแถวกับปุ่ม เพื่อรักษาข้อความปุ่มให้อ่านต่อเนื่อง

ตรวจข้อมูลก่อนเปลี่ยนขั้น ย้อนกลับแล้วข้อมูลยังอยู่ และย้าย focus ไปหัวข้อขั้นใหม่ แสดงข้อผิดพลาดใกล้จุดดำเนินการพร้อมข้อความที่เทคโนโลยีช่วยอ่านรับรู้ได้ ห้ามนำฟอร์มย่อเดิมกลับมาแทรกในหน้าเมนเทอร์

## 6. Responsive และการเข้าถึง

จุดเปลี่ยน layout ปัจจุบันต่างกันตามความหนาแน่น: 1000px สำหรับใบสมัคร, 900px สำหรับรายละเอียด, 700px สำหรับ layout ทั่วไป และ 480px สำหรับปุ่มใบสมัคร ไม่สร้าง breakpoint ใหม่หากปรับจากของเดิมได้

- ตรวจจริงที่ 390, 768 และ 1440px รวมความกว้างใกล้ breakpoint ที่มีการแก้ไข
- ไม่มี horizontal scroll ทั้งหน้า; หมวด ตัวเลือก และ metadata ต้อง wrap ได้
- ลำดับเนื้อหาใน DOM ต้องอ่านรู้เรื่องเมื่อเหลือคอลัมน์เดียว
- ทุก action ใช้คีย์บอร์ดได้ มี focus ที่มองเห็น และมีชื่อที่สื่อความหมาย
- ใช้ skip link, main, heading และ label อย่างถูกต้อง ไม่ตัด outline ของ controls โดยไม่มีสิ่งทดแทน
- รองรับ reduced motion; ไม่ทำให้การเข้าถึงข้อมูลต้องรอ animation
- ตรวจ contrast และข้อความไทยจากภาพ render จริง การผ่านเครื่องมือตรวจอัตโนมัติไม่ใช่การยืนยันว่าผ่านทุกด้าน

## 7. ความน่าเชื่อถือและสถานะระบบ

ความน่าเชื่อถือมาจากการบอกข้อมูลที่จำเป็นก่อนให้ผู้ใช้ตัดสินใจ: ประสบการณ์ หลักฐาน ขอบเขตการช่วย ราคา ระยะเวลา และข้อมูลใดจะแสดงสาธารณะ ป้าย “ยืนยันแล้ว” ใช้ได้เมื่อมีการตรวจจริง หรือมีบริบทชัดว่าเป็นข้อมูลตัวอย่าง

ใน prototype ต้องบอกว่าข้อมูลเวที เมนเทอร์ อันดับ และราคาเป็นสมมติ การเลือกไฟล์เป็นการทดลองในหน้า ไม่มีการอัปโหลดจริง การส่งใบสมัครต้องใช้คำว่า “ตัวอย่าง” และอธิบายว่าข้อมูลไม่ถูกส่ง ฟอร์มปัจจุบันเก็บข้อมูลระหว่างอยู่ในหน้าเท่านั้น

ข้อความเรื่องการชำระเงิน คืนเงิน และการอนุมัติในตัวอย่างเป็นเนื้อหาออกแบบ flow ไม่ใช่หลักฐานว่าระบบรองรับบริการนั้นแล้ว ก่อนใช้งานจริงต้องให้เนื้อหาและพฤติกรรมตรงกับระบบที่ส่งมอบ

## 8. ข้อควรปรับต่อจากการวิเคราะห์

รายการนี้เป็นแนวทางรอบถัดไป เอกสารนี้ยังไม่ได้แก้ UI ในรายการเหล่านี้:

- `mentor-application.css` ใช้ theme tokens สำหรับสีหลัก เส้นแบ่ง และข้อความรองแล้ว ที่เหลือเป็นสีตกแต่งเฉพาะหน้า ได้แก่ gradient ของ hero และ preview, พื้นอ่อน `#FBF9FD` และ `#FCFBFD`, ขอบ note `#CEB8EB`, ขอบ hover ของตัวเลือก `#B896DF`, สี focus ring `#B598DE`, สีข้อความ `#2C2235` และ `#6C5A7D`, พื้น avatar `#E3D3F7` และสีข้อผิดพลาด `#A43148` ให้เพิ่มเป็น token เมื่อมีหน้าอื่นต้องใช้ค่าเดียวกัน
- สีข้อผิดพลาดยังต่างกันระหว่างฟอร์มเมนเทอร์ `#A3341F` กับใบสมัคร `#A43148` ควรรวมเป็นค่าเดียวเมื่อแก้ทั้งสองหน้าพร้อมกัน
- `--muted-soft` ให้คอนทราสต์ 4.48:1 บนพื้น `--purple-tint` ซึ่งไม่ผ่าน AA ข้อความเล็กบนพื้นโทนม่วงให้ใช้ `--muted` แทน
- ทำขนาดปุ่ม ไอคอน และ focus ให้สม่ำเสมอ โดยเฉพาะ controls ขนาดเล็กและ header บนมือถือ
- `legend` ระดับ global ตั้ง `margin-bottom: 12px` แต่ Chrome เว้นที่ให้ legend ตามกลไก `fieldset` อยู่แล้ว แผงตัวกรองจึงต้องตั้งกลับเป็น 0 ถ้าเพิ่ม fieldset ที่อื่นให้ระวังจุดเดียวกัน
- ข้อมูลตัวอย่างยังไม่ครอบหมวด `performing` `film` และ `food` ชิปสามหมวดนี้จะพาไปหน้าว่าง ถือว่าถูกต้องจนกว่าจะมีข้อมูลจริงตาม [organiser-submission.md](organiser-submission.md)
- รักษาการค้นหาให้เข้าถึงง่าย แม้เพิ่มข้อมูล highlights ในอนาคต
- ทบทวนข้อความเล็กและสีอ่อนจากบริบทใช้งานจริง ไม่ลด contrast เพื่อทำให้หน้าดูเบา

## 9. วิธีใช้เอกสารนี้ในการพัฒนา

ก่อนสร้างหน้าใหม่ อ่านเอกสารนี้และเลือก component เดิมที่ใช้ซ้ำได้ เริ่มจากข้อมูลและ action หลัก แล้วกำหนด layout สำหรับมือถือก่อนขยายเป็น desktop ใช้ tokens เดิมและเพิ่ม styles เฉพาะเท่าที่จำเป็น หากจำเป็นต้องเพิ่มรูปแบบหลักใหม่ ให้บันทึกเหตุผลและกฎใช้งานในเอกสารนี้

ก่อนส่งงาน ให้ตรวจเส้นทางหลัก สถานะว่าง/ผิดพลาด/เลือกแล้ว/สำเร็จตามขอบเขตที่เปลี่ยน ทดสอบคีย์บอร์ด ตรวจภาพ render จริงทั้งสามขนาด และ build เมื่อแก้โค้ด ไม่จำเป็นต้องรัน browser tests ใหม่สำหรับการแก้เอกสารล้วน

แหล่งอ้างอิงใน repo:

- Theme และ components: [styles.css](src/styles.css), [Layout.tsx](src/components/Layout.tsx)
- ใบสมัคร: [MentorApplication.tsx](src/pages/MentorApplication.tsx), [mentor-application.css](src/mentor-application.css)
- ภาพอ้างอิง: [หน้าแรก desktop](artifacts/home-desktop.png), [เมนเทอร์ desktop](artifacts/mentors-desktop.png), [ใบสมัคร desktop](artifacts/application-desktop-step-1.png), [ใบสมัคร mobile](artifacts/application-mobile-step-4.png)
- ผลตรวจและข้อจำกัด: [QA.md](QA.md)

## 10. แชตกลุ่มกับเมนเทอร์

- คงธีมม่วง–ทอง: ข้อความของเราเป็นพื้นม่วง ตัวอักษรขาว ข้อความสมาชิกเป็นการ์ดขาว และโจทย์ทีมปักด้านบนด้วยพื้นทองอ่อน
- เดสก์ท็อปวางบทสนทนาคู่แผงสมาชิกและนัดหมาย แท็บเล็ตวางแผงสองคอลัมน์ใต้แชต มือถือเรียงหนึ่งคอลัมน์ มีลิงก์ข้ามไปสมาชิก
- จำกัดความกว้างฟองข้อความและตัดข้อความยาวได้ ชื่อไฟล์และอีเมลต้องไม่ทำให้หน้าล้น ใช้กล่องเลื่อนสำหรับประวัติและปุ่มโหลดข้อความก่อนหน้า
- แสดงชื่อผู้ส่ง เวลา และจำนวนผู้อ่าน คงตำแหน่งเมื่อกำลังอ่านย้อนหลัง และเลื่อนลงเมื่อผู้ใช้อยู่ท้ายบทสนทนา
- เจ้าของกลุ่มเห็นปุ่มเชิญและนำสมาชิกออก สมาชิกคนอื่นเห็นคำอธิบายสิทธิ์ การซ่อนปุ่มต้องมีการตรวจสิทธิ์ที่ API ด้วย
- ลิงก์คอลเปิดหน้าต่างใหม่พร้อมสัญลักษณ์กำกับ ฟอร์มนัดหมายระบุว่าใช้เวลาในเครื่อง ไม่แสดงว่ามีการชำระเงินหรือยืนยันคิวจากการสร้างกลุ่ม
- เมนูหลักตัดขึ้นบรรทัดใหม่เป็นรายการ ห้ามตัดคำภายในชื่อเมนูเมื่อเพิ่มเมนูแชต

รายละเอียดพฤติกรรมและข้อจำกัดอยู่ใน [chat.md](chat.md) ภาพตรวจรับอยู่ที่ `artifacts/chat-{desktop,tablet,mobile}.png`
