# ChampionWays — ระบบให้ผู้จัดงานลงงานแข่งฟรี

สถานะ: ข้อเสนอการออกแบบ ยังไม่ได้ลงมือทำ เขียนเมื่อ 12 กันยายน 2569

เอกสารนี้ออกแบบทางที่ ChampionWays จะได้ข้อมูลงานแข่งขัน**จริง** มาแทนข้อมูลสมมติ 14 รายการใน [competitions.ts](src/data/competitions.ts) โดยให้ผู้จัดงานลงประกาศเองได้ฟรี ใช้ร่วมกับ [design.md](design.md) สำหรับสี ตัวอักษร และ components ถ้ามีการตัดสินใจใหม่ให้แก้เอกสารนี้ให้ตรงด้วย

## 1. ทำไมต้องเป็นแบบนี้

ข้อมูลงานแข่งคือสินค้าหลักของเว็บ ถ้าไม่มีรายการจริงก็ไม่มีเหตุผลให้ใครกลับมา แต่วิธีได้ข้อมูลมามีแค่ไม่กี่ทางและส่วนใหญ่ใช้ไม่ได้

จากการศึกษา [Contester.Life](https://contester.life/) ซึ่งเป็นเว็บรวมงานแข่งไทยที่ทำงานอยู่จริง พบว่าเขาไม่ได้ดูดข้อมูลจากใคร แต่**เปิดให้ผู้จัดลงเอง** ผ่าน `/contest/submit` หลังล็อกอิน มีแบนเนอร์ชวนผู้จัดบนหน้าแรก และมีหน้า Guidelines กับ Partner With Us ใน footer ส่วนหน้า About เขียนว่ารวบรวม "from various sources" แปลว่าทีมงานคัดเองด้วยช่วงเริ่มต้น รายได้มาจากการโปรโมทให้ผู้จัดและเงินบริจาค ไม่ใช่ค่าลงประกาศ

ข้อกำหนดการใช้งานของเขาข้อ 6.2 ระบุว่าเนื้อหาบนเว็บเป็นทรัพย์สินของ Contester.Life ใช้ได้เฉพาะส่วนบุคคลและไม่ใช่เชิงพาณิชย์ ห้ามทำซ้ำหรือแจกจ่ายโดยไม่ได้รับอนุญาตเป็นลายลักษณ์อักษร และ `robots.txt` ปิด `/api/` ไว้ **การดึงข้อมูลจากเว็บรวมงานแข่งเจ้าอื่นมาใส่ ChampionWays จึงทำไม่ได้** ทางที่เหลือคือหาข้อมูลเองให้ถูกต้อง

## 2. หลักการที่ห้ามผิด

- **ฟรีสำหรับผู้จัดเสมอ** การลงประกาศไม่มีค่าใช้จ่ายและไม่มีคิวพิเศษสำหรับคนจ่ายเงิน ถ้าจะหารายได้ในอนาคตให้หาจากบริการอื่น ไม่ใช่จากการกั้นไม่ให้งานขึ้นหน้าเว็บ
- **ทุกรายการต้องมีลิงก์ต้นทาง** ต้องกรอก `sourceUrl` ชี้ไปประกาศทางการของผู้จัดเสมอ ผู้ใช้ตรวจสอบเองได้ และเราไม่ต้องเป็นแหล่งข้อมูลสุดท้าย
- **เก็บข้อเท็จจริง ไม่คัดลอกคำบรรยาย** ชื่องาน วันปิดรับ เงินรางวัล คุณสมบัติ เป็นข้อเท็จจริงที่ไม่มีลิขสิทธิ์ ส่วนคำบรรยายต้องเขียนใหม่หรือมาจากผู้จัดเอง ห้ามคัดลอกข้อความจากเว็บอื่น
- **ตรวจก่อนเผยแพร่** ไม่มีรายการไหนขึ้นหน้าเว็บอัตโนมัติ
- **บอกที่มาให้ผู้ใช้เห็น** การ์ดและหน้ารายละเอียดต้องบอกว่าข้อมูลมาจากผู้จัดโดยตรงหรือทีมงานคัดมา และตรวจครั้งล่าสุดเมื่อไหร่

## 3. แหล่งข้อมูลสามชั้น

ทำตามลำดับ ชั้นแรกใช้เริ่มต้น ชั้นที่สองคือเครื่องยนต์ระยะยาว

**ชั้น 1 — ทีมงานคัดเองจากต้นทาง (`source: 'editorial'`)**
ไปที่ประกาศของผู้จัดโดยตรง เช่น เพจคณะ มหาวิทยาลัย มูลนิธิ หรือบริษัท แล้วกรอกข้อเท็จจริงเอง เขียนคำบรรยายใหม่ ใช้บูตสแตรปให้ได้ 20–30 รายการจริงก่อนเปิดรับจากภายนอก เพราะเว็บที่ว่างเปล่าไม่มีผู้จัดคนไหนอยากลง

**ชั้น 2 — ผู้จัดลงเอง (`source: 'organiser'`)**
ฟอร์มสาธารณะตามข้อ 5 นี่คือทางที่ยั่งยืน ข้อมูลเป็นของเราถูกต้องตามกฎหมายเพราะเจ้าของข้อมูลส่งมาเอง และได้ความสดโดยไม่ต้องไล่ตามเอง

**ชั้น 3 — พาร์ตเนอร์ (`source: 'partner'`)**
ถ้ามีเว็บหรือหน่วยงานที่ยินดีแบ่งปันข้อมูลอย่างเป็นทางการ ให้ทำข้อตกลงเป็นลายลักษณ์อักษรก่อน ไม่ควรพึ่งเป็นทางหลักเพราะควบคุมความสดไม่ได้

## 4. โครงสร้างข้อมูล

โครงสร้าง `Competition` ที่รองรับข้อมูลจริง หมวดหมู่ 13 หมวด และระบบตัวกรอง อยู่ใน [competition-model.md](competition-model.md) ซึ่งเป็นแหล่งอ้างอิงเดียวของเรื่องนี้ อย่าเขียนซ้ำในเอกสารนี้

ฟิลด์ที่เกี่ยวกับการลงประกาศโดยเฉพาะ และไม่ได้อยู่ใน `Competition` ให้เก็บแยกเป็น `CompetitionSubmission` เพราะเป็นข้อมูลของ "ใบที่ส่งเข้ามา" ไม่ใช่ "รายการที่เผยแพร่แล้ว" ถ้าใส่รวมกัน `Competition` จะบวมด้วยฟิลด์ที่ทุกหน้าไม่ได้ใช้

- `organizerName` `organizerEmail` `organizerPhone` `organizerRole` — ช่องทางติดต่อกลับ อีเมลกับเบอร์ใช้ตรวจสอบเท่านั้น ไม่แสดงสาธารณะ
- `status: 'draft' | 'pending' | 'published' | 'rejected' | 'closed'` — สถานะตามข้อ 6
- `submittedAt` `reviewedAt` `reviewNote` — ร่องรอยการตรวจ
- `consent` — บันทึกว่าติ๊กยอมรับข้อไหนไว้เมื่อไหร่

เมื่อใบสมัครผ่านการตรวจ ระบบจะสร้าง `Competition` จากใบนั้น โดย `source` เป็น `'organiser'` และ `lastVerifiedAt` เป็นวันที่ตรวจ

## 5. ฟอร์มลงงานแข่ง 4 ขั้นตอน

ใช้โครงเดียวกับ [MentorApplication.tsx](src/pages/MentorApplication.tsx) ทุกอย่าง คือ `fieldset` ต่อขั้นพร้อม `data-stage` ซ่อนด้วย `hidden` และ `disabled` ย้าย focus ไปที่ `h2` ของขั้นใหม่ แถบขั้นตอนด้านซ้าย ตรวจความถูกต้องตอนกดถัดไป และแสดงข้อความผิดพลาดด้วย `role="alert"` ส่วน CSS ใช้ `.group` `.group-head` `.review-group` และ token จาก [styles.css](src/styles.css) ที่มีอยู่แล้ว ไม่สร้างระบบใหม่

**ขั้น 1 — ผู้จัดงาน**
ชื่อหน่วยงาน ชื่อผู้ติดต่อ อีเมล เบอร์โทร ตำแหน่ง และลิงก์เว็บหรือเพจทางการ ต้องบอกให้ชัดว่าอีเมลกับเบอร์ใช้ยืนยันตัวตนเท่านั้น ไม่ขึ้นหน้าเว็บ ตรงตามหลักใน [design.md](design.md) ข้อ 7 ที่ว่าผู้กรอกต้องรู้ก่อนว่าข้อมูลไหนจะแสดงสาธารณะ

**ขั้น 2 — รายละเอียดงาน**
ชื่องาน หมวด (เลือกได้หลายหมวด ใช้ `.topic` แบบเดียวกับหัวข้อความถนัดของเมนเทอร์ ซึ่งเป็นเป้าแตะเต็มแถวสูง 44px) คำบรรยายสั้นสำหรับการ์ด ระดับผู้เข้าแข่ง ขนาดทีม และรายละเอียดยาว มีตัวนับอักษรด้วย `.count` เพื่อกันคำบรรยายที่ยาวจนการ์ดพัง

**ขั้น 3 — วันเวลา รางวัล และลิงก์**
วันเปิดรับ วันปิดรับ วันจัดงาน รูปแบบออนไลน์หรือสถานที่ เงินรางวัล ค่าสมัคร ลิงก์ประกาศทางการ และลิงก์สมัคร ช่องเงินรางวัลใช้ `.price-row` คู่กับ `.price-preview` เหมือนหน้าเมนเทอร์ เพื่อให้เห็นทันทีว่าจะแสดงผลออกมาเป็นอะไร และมีตัวเลือก "ไม่มีเงินรางวัล" ชัดเจนแทนการปล่อยว่าง

**ขั้น 4 — ตรวจทานและยืนยัน**
แสดงพรีวิวการ์ดแบบที่จะขึ้นจริง ตามด้วยกลุ่มข้อมูลแต่ละชุดพร้อมปุ่ม "แก้ไข" ที่กระโดดกลับไปขั้นนั้นโดยข้อมูลไม่หาย ปิดท้ายด้วยรายการยินยอมที่ต้องติ๊กครบทุกข้อ

- ยืนยันว่าเป็นผู้จัดงานนี้หรือได้รับมอบหมายให้ประชาสัมพันธ์
- ยืนยันว่าข้อมูลถูกต้อง และจะแจ้งทันทีเมื่อมีการเปลี่ยนวันหรือยกเลิก
- ยอมรับว่าข้อความและภาพที่ส่งมาไม่ละเมิดลิขสิทธิ์ผู้อื่น และอนุญาตให้ ChampionWays เผยแพร่เพื่อประชาสัมพันธ์งานนี้
- รับทราบว่าทีมงานตรวจสอบก่อนเผยแพร่ และอาจแก้ถ้อยคำให้เข้ารูปแบบเว็บโดยไม่เปลี่ยนสาระ
- รับทราบว่าการลงประกาศไม่มีค่าใช้จ่าย และไม่มีการรับประกันจำนวนผู้สมัคร

ใช้กฎเดียวกับใบสมัครเมนเทอร์คือ **ถ้าแก้ข้อมูลหลังติ๊ก "ข้อมูลถูกต้อง" แล้ว ให้ติ๊กนั้นหลุดออก** เพื่อบังคับให้อ่านซ้ำ และเมื่อขาดข้อใด ให้ย้าย focus ไปที่ช่องแรกที่ยังไม่ติ๊ก

## 6. สถานะรายการและการตรวจ

```
ผู้จัดกรอก → pending → ทีมตรวจ ─┬→ published → (ถึงวันปิดรับ) → closed
                                 ├→ rejected (พร้อมเหตุผลทางอีเมล)
                                 └→ pending (ขอข้อมูลเพิ่ม)
```

ทีมตรวจอะไรบ้าง

1. `sourceUrl` เปิดได้จริง และเป็นประกาศของงานนี้จริง
2. ชื่อหน่วยงานในฟอร์มตรงกับที่ปรากฏในประกาศ
3. วันปิดรับยังไม่ผ่าน และสอดคล้องกับต้นทาง
4. เงินรางวัลกับค่าสมัครตรงกับต้นทาง
5. คำบรรยายไม่ใช่ข้อความคัดลอกมาทั้งก้อนจากเว็บอื่น
6. ไม่ใช่การขายของหรือรับสมัครงานที่แฝงมาเป็นการแข่งขัน

เป้าหมายเวลาตรวจคือภายใน 2 วันทำการ และแจ้งผลทางอีเมลทุกกรณีรวมถึงกรณีไม่ผ่าน พร้อมบอกเหตุผลและวิธีแก้

## 7. กันสแปมโดยไม่กันคนจริง

- ต้องยืนยันอีเมลก่อนใบสมัครเข้าคิวตรวจ กันบอทได้เกือบทั้งหมดโดยไม่ต้องใช้ captcha
- จำกัดจำนวนใบต่ออีเมลต่อวัน
- ห้าม `sourceUrl` เป็นลิงก์ย่อ ต้องเป็นโดเมนจริงที่ตรวจสอบได้
- ผู้จัดที่ผ่านการตรวจแล้วครั้งหนึ่ง ครั้งต่อไปเข้าคิวเร็วขึ้น แต่ยังต้องตรวจอยู่ดี
- honeypot field ที่คนมองไม่เห็น ถ้ามีค่าแปลว่าเป็นบอท

## 8. ความสดของข้อมูล

ข้อมูลค้างทำลายความเชื่อถือเร็วกว่าข้อมูลน้อย

- ถึง `closesAt` แล้วให้ย้ายไป `closed` อัตโนมัติและออกจากรายการหลัก แต่หน้ารายละเอียดยังเปิดได้เพื่อไม่ให้ลิงก์เสีย
- แสดง "ตรวจล่าสุดเมื่อ ..." จาก `lastVerifiedAt` บนหน้ารายละเอียด
- รายการที่ `source` เป็น `editorial` และไม่ได้ตรวจเกิน 30 วัน ให้ขึ้นเตือนในหน้าจัดการของทีม
- ส่งอีเมลถามผู้จัดก่อนถึงวันปิดรับ 7 วันว่ายังตามกำหนดเดิมหรือไม่ ปุ่มตอบมีสองปุ่มคือ "ยืนยันตามเดิม" กับ "มีการเปลี่ยนแปลง"

## 9. หน้าและ route ที่ต้องเพิ่ม

เพิ่มใน [main.tsx](src/main.tsx)

| Route | หน้า | ใครเข้า |
| --- | --- | --- |
| `/organizers` | อธิบายว่าลงฟรี ลงยังไง ตรวจอะไรบ้าง ใช้เวลาเท่าไหร่ | ผู้จัดที่ยังลังเล |
| `/organizers/submit` | ฟอร์ม 4 ขั้นตามข้อ 5 | ผู้จัด |
| `/organizers/guidelines` | เกณฑ์รับและไม่รับ เขียนเป็นภาษาคน | ผู้จัด |

และเพิ่มทางเข้าที่หน้าแรกกับ footer ให้ผู้จัดหาเจอ โดยไม่แย่งพื้นที่จากนักเรียนซึ่งเป็นผู้ใช้หลัก ตำแหน่งที่เหมาะคือแถบเล็กท้ายรายการงานแข่ง ไม่ใช่ hero

## 10. ข้อกฎหมายและความเป็นส่วนตัว

- อีเมลและเบอร์โทรของผู้ติดต่อเป็นข้อมูลส่วนบุคคลตาม PDPA ต้องแจ้งวัตถุประสงค์ก่อนเก็บ เก็บเท่าที่ใช้ตรวจสอบและติดต่อกลับ และต้องมีทางขอลบ
- ต้องมีหน้าเงื่อนไขการใช้งานและนโยบายความเป็นส่วนตัวของ ChampionWays เองก่อนเปิดรับข้อมูลจริง
- ผู้จัดให้สิทธิ์เราเผยแพร่ข้อมูลงานของเขาเพื่อประชาสัมพันธ์ โดยลิขสิทธิ์ยังเป็นของเขา ระบุให้ชัดในข้อความยินยอมข้อ 3 ของข้อ 5
- ภาพโปสเตอร์ที่ผู้จัดอัปโหลดต้องเป็นภาพที่เขามีสิทธิ์ ถ้าไม่แน่ใจให้ใช้การ์ดข้อความแทน

## 11. ขอบเขตของ prototype

ตอนนี้เว็บยังไม่มี backend ฟอร์มเมนเทอร์จึงเก็บข้อมูลไว้ในหน้าเท่านั้นและใช้คำว่า "ตัวอย่าง" ฟอร์มลงงานแข่งต้องทำแบบเดียวกัน คือ**ทำ UI และ validation ให้ครบ แต่บอกตรง ๆ ว่ายังไม่ส่งข้อมูลจริง** ห้ามเขียนข้อความที่ทำให้ผู้จัดเข้าใจว่าลงแล้วงานจะขึ้นเว็บ

สิ่งที่ต้องมีจริงก่อนเปิดใช้งาน ได้แก่ ที่เก็บข้อมูล ระบบยืนยันอีเมล หน้าจัดการสำหรับทีมตรวจ และการส่งอีเมลแจ้งผล

## 12. ลำดับการทำ

1. ปรับ `Competition` ตามข้อ 4 และแก้ข้อมูลสมมติเดิมให้เข้ารูปแบบใหม่ พร้อมขยายหมวด
2. ทำ UI ให้รองรับฟิลด์ใหม่ คือป้ายที่มา วันตรวจล่าสุด ลิงก์ต้นทาง และกรณีไม่มีเงินรางวัล
3. ทำหน้า `/organizers` และ `/organizers/guidelines`
4. ทำฟอร์ม `/organizers/submit` 4 ขั้นในโหมดตัวอย่าง พร้อมเทส Playwright แบบเดียวกับ [mentor-application.spec.ts](tests/mentor-application.spec.ts)
5. คัดข้อมูลจริง 20–30 รายการตามชั้น 1 ของข้อ 3 แทนข้อมูลสมมติ
6. ต่อ backend เมื่อพร้อม

ขั้น 1 ถึง 4 ทำได้ทันทีโดยไม่ต้องมี backend

## 13. อ้างอิงใน repo

- โครงสร้างข้อมูลปัจจุบัน: [competitions.ts](src/data/competitions.ts)
- ฟอร์มที่ใช้เป็นต้นแบบ: [MentorApplication.tsx](src/pages/MentorApplication.tsx), [mentor-application.css](src/mentor-application.css)
- แนวทางดีไซน์และกฎความน่าเชื่อถือ: [design.md](design.md)
- เทสฟอร์ม: [mentor-application.spec.ts](tests/mentor-application.spec.ts)
- ข้อจำกัดที่รู้แล้ว: [QA.md](QA.md)
